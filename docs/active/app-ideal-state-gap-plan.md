# One Person Lab App Ideal-State Gap Plan

Owner: `one-person-lab-app`
Purpose: `app_ideal_state_gap_plan`
State: `active_plan`
Machine boundary: Human-readable active truth and gap plan. Machine-readable truth lives in `contracts/`, source, release artifacts, updater metadata, and test results.
Last reviewed: `2026-06-21`

2026-06-30 SSOT refresh: this active plan keeps App functional / structural
gaps only: App-owned contracts, active-shell sync, GUI definition stack,
Settings / Storage, release-boundary validators, shell-candidate policy, route
receipts, first-run policy, and runtime/domain no-authority guards. Release
cohorts, clean-VM proof, packaged `.app` smoke, same-cohort user-path artifacts,
real user-directory E2E, owner acceptance, and production-ready claims are
separate release / lifecycle evidence lanes. They should not be mixed into the
ideal-state or current-state gap list, and App validation or shell tests cannot
replace them.

Functional and structural lanes come first: App-owned contracts, active-shell
adapter sync, AionUI mainline GUI behavior, Hermes foreground-alternative
convergence, page-state matrices, route receipts, first-run policy, Settings /
Storage, release-boundary validators, and runtime/domain no-authority guards.
Release cohorts, clean-VM evidence, packaged `.app` smoke, same-cohort user-path
artifacts, real user-directory E2E, owner acceptance, and production-ready
claims are not maintained in this active plan. They remain mandatory for release
or production claims, but they must not block independent App contract/shell
cleanup, and App validation or shell tests cannot replace them.

## Current Completion Progress

| Area | Current status | Current readout |
| --- | --- | --- |
| App product boundary | `active_truth` | App owns desktop packaging, release assets, updater metadata, first-run checks, screenshots, user guides, App contracts, and GUI page-state validation. |
| Purpose-first App role | `active_contract_truth` | The App is the purpose-first Codex wrapper/product truth for ordinary research, grant, presentation, book, and general work entry. Runtime truth, domain readiness, owner receipts, memory/artifact bodies, and family production readiness remain outside App ownership. |
| Framework / domain split | `active_truth` | OPL Framework owns runtime/provider/read-model/action execution; MAS/MAG/RCA/BookForge/OMA own domain truth, quality/export verdicts, memory body, artifact body, owner receipts, and typed blockers. |
| Active shell boundary | `active_external_checkout` | `shells/aionui/` is an external checkout from `gaofeng21cn/opl-aion-shell`; shell implementation history is not merged into the App repo default branch. |
| Product contracts | `landed_contract_backed` | `contracts/app-product-profile.json`, `contracts/app-page-state-matrix.json`, `contracts/app-first-run-test-matrix.json`, and `contracts/app-release-channel.json` hold App-owned machine policy. Release evidence remains cohort-bound and artifact-backed. |
| Runtime progress projection | `landed_contract_backed` | Runtime display consumes OPL shared progress projection fields only: `deliverable_progress_delta`, `platform_repair_delta`, and `progress_delta_classification`. Platform repair is infrastructure repair and cannot be displayed as deliverable, paper, manuscript, or submission progress. |
| Runtime provider readiness repair readout | `landed_contract_and_test_backed` | Runtime display consumes OPL provider readiness refs as infrastructure-only repair guidance. `worker_not_ready` surfaces `opl family-runtime worker start --provider temporal`; missing Temporal Search Attributes surface `opl family-runtime provider repair --provider temporal`. These commands stay provider readiness repair, not domain readiness, owner receipt authority, typed blocker authority, App release readiness, or a replacement for `current_owner_delta`. |
| Runtime artifact-native drilldown | `landed_contract_and_test_backed` | Runtime task drilldown consumes OPL Stage Artifact Kernel workbench refs for current pointer, canonical artifacts, export artifacts, lineage, retention, conformance, manifest/hash/receipt/blocker refs. It remains refs-only and cannot read artifact body, own artifact authority, or declare domain quality/export/readiness, App release readiness, or family production readiness. |
| Runtime owner-action default | `landed_contract_and_test_backed` | Runtime page default attention is user-task-status / owner-delta first: running, active, queued, attention, each task's next visible step, next owner, required delta, accepted return shape, readiness false flags, and compact count summary. Active project lines come from App-consumed OPL refs and must preserve `status`, `active_run_id`, and `next_visible_step` without claiming active worker run, domain readiness, App release readiness, production readiness, or paper quality. `contracts/app-runtime-bridge.json` and `app-gui-product-contract.json` require `default_operator_payload=current_owner_delta` and no longer expose a compact owner-delta compatibility payload. Full ledger detail and provider diagnostics are on-demand diagnostic, audit, or release evidence only. |
| Claude Science task-awareness landing | `landed_mainline` | Claude Science learning now lands on the existing Runtime task projection. Framework main `dd867215` produces refs-only task awareness and dry-run action preview readbacks; App main `014e0f8` contracts Runtime/global and current-task slices with validation; App main `b7cd8ed` records landing status; AionUI main `6bccbab8f` renders Runtime task ref summaries and `ffb5701e0` renders conversation current-task inline status plus right-side workspace/inspector evidence refs. This is not a new dashboard or shell-owned task system, and it is not release-ready or domain-ready evidence. |
| Agent installation exposure | `landed_contract_and_live_root_guard_backed` | `contracts/app-install-exposure-policy.json` now owns `agent_installation_contract`: MAS/MAG/RCA are plugin-packaged skill entries with direct skill compatibility, BookForge and OMA remain OPL-generated skill surfaces, and `npm run validate:agent-installation` can verify both real plugin roots and a Codex skills root for duplicate bare MAS/MAG/RCA/BookForge mirrors. |
| Release user-path evidence | `cohort_bound_refs_only` | OPL App/operator summary refs can support a same-cohort App release review, but they are refs-only user-path evidence, not App release-ready, domain-ready, or family production-ready authority. |
| Codex App positioning | `active_contract_truth` | The App ordinary path is a Codex CLI fixed-executor experience with built-in MAS/MAG/RCA/BookForge purpose entries; AionUI multi-backend choices are implementation material, not the user-facing App model. |
| GUI 定义栈 | `active_definition` | `docs/product/gui/ideal-interaction-spec.md` 定义理想 Codex App 形态交互模型，`docs/product/gui/codex-to-opl-app-delta.md` 定义 OPL 专用产品增量，`docs/product/gui/feature-inventory.md` 继续维护跨 shell 能力清单。 |
| Active shell upstream intake | `landed_verified_with_checkout_drift_guard` | `contracts/app-shell-adapter.json#shell_source.upstream_ref` records the current active shell upstream ref; shell implementation history stays in `gaofeng21cn/opl-aion-shell`. Local App validation reads `shells/aionui`, which is an ignored external shell checkout and may be a symlink to the canonical sibling checkout; if a local checkout is stale, check the shell source repo before treating active-shell validation as App contract drift. |
| GUI mainline / alternative policy | `landed_structure_readback_backed` | AionUI is the App GUI mainline. Hermes Desktop is the only foreground alternative candidate. AGUI/CopilotKit remains archived technical proof and is not a routine implementation, validation, or polish lane. `scripts/validate-shell-candidates.ts` derives the default foreground scope, archived technical-proof set, update policy, release participation, and adapter role expectations from `contracts/app-shell-candidates.json#alternative_gui_policy` rather than hard-coding AGUI as a foreground-policy special case. `scripts/validate-shell-convergence.ts` now aggregates the App-owned shell policy readback and returns `closed_structure_gate_not_live_evidence` only when the active AionUI adapter, Hermes default validation scope, AGUI no-resurrection guard, and false-ready boundary agree. AGUI `candidate` filenames and manifest fields are compatibility labels for explicit replay only; they must not seed default worklists. The generic shell adoption gate is shell-agnostic; React/CopilotKit requirements belong only to explicit AGUI replay evidence. |
| Home shell conformance | `landed_contract_backed` | App contracts and shell tests now require the home input to hide executor/backend/provider/permission selectors, keep the App-owned model selector/status visible, and show MAS/MAG/RCA/BookForge as purpose-first entries. |
| Conversation shell conformance | `landed_contract_backed` | App contracts now require ordinary Codex conversations to keep backend/provider/permission selectors hidden after send while preserving the App-owned model selector/status; shell tests cover the conversation header and sendbox. |
| Codex CLI tool-output rendering | `landed_shell_tested` | The active shell normalizes Codex ACP `raw_output` from `aggregated_output`, `formatted_output`, `stdout`, and `stderr` fields and preserves newline-bearing tool output in the conversation view; the shell regression test covers the newline case. |
| Built-in assistant route receipt | `landed_release_gate_backed` | App contracts and release evidence validation now require MAS/MAG/RCA/BookForge packaged GUI route smoke to persist a Codex CLI `builtin_capability` route receipt so selection is observable beyond the UI badge. |
| Settings diagnostics partition | `landed_contract_backed` | App contracts and product profile now require ordinary Settings groups to be Overview, Setup & Access, Capabilities, Maintenance & Updates, Data & Storage, Preferences, and Advanced, with About/Update/Theme as secondary surfaces. Legacy overview/runtime/system/model/agent/assistants/skills-hub/tools/display/webui/pet routes redirect to App-owned pages, and active-shell validation plus shell tests cover the modal, router, Storage route, and Guid Access shortcut. |
| Settings / model policy single source | `landed_contract_backed_non_live` | `contracts/app-settings-control-plane.json#model_reasoning_policy_source` points Settings model/account UI to App product profile and GUI executor policy. Aion/Hermes/shell may render only App-derived model and reasoning policy; release cohort, clean-VM, live provider access, and owner acceptance remain separate evidence lanes. |
| Docker WebUI beginner functional path | `landed_contract_and_guide_backed_non_live` | `contracts/app-install-exposure-policy.json#installer_surfaces.docker_webui.installer_model` now names the beginner non-live path: one-click installer, browser Access API key entry, configure-codex runtime proxy smoke, startup doctor diagnostics, failure recovery, and operator progress surfaces. The guide manifest and release-boundary tests validate the contract; this is not release-ready or clean-VM evidence. |
| Local data lifecycle | `landed_contract_and_shell_test_backed` | `contracts/app-release-channel.json#local_data_lifecycle` owns updater cache, conversation artifacts, OPL Runtime Fabric roots, and logs. Settings / Storage inventory, conversation archive/restore-proof/delete receipt, runtime pointer-prune dry-run/execute receipt, log bounded-rotation dry-run/execute receipt, and updater cache cleanup receipt are implemented in the active shell and covered by focused tests. Issue #5 landing provenance is archived in `docs/history/process/local-data-lifecycle-issue-5.md`. This is not packaged App smoke, real user-directory E2E, release-ready, or owner acceptance evidence. |
| Docs lifecycle | `single_active_truth_owner_summary_covered` | This file holds current App product gaps and next-round baton; `docs/status.md`, `docs/project.md`, `docs/architecture.md`, `docs/invariants.md`, and `docs/decisions.md` hold durable current truth. App `README*` and `docs/**/*.md` docs-governance coverage is compressed in `docs/history/process/README.md`; open App work is release / install / GUI implementation and evidence-tail work under the listed owners, not an unreviewed-docs backlog. |

## Current-State vs Ideal-State Gaps

| Gap | Current state | Completion gate |
| --- | --- | --- |
| `runtime_page_operator_evidence` | Runtime page consumes OPL App/operator drilldown, refs-only safe action routes, provider readiness repair refs, and Stage Artifact Kernel workbench refs. The active project line projection, provider readiness repair projection, and artifact-native drilldown projection are contract-backed in `contracts/app-runtime-bridge.json`, `contracts/app-page-state-matrix.json`, and `contracts/app-gui-product-contract.json`; `scripts/validate-active-shell.ts --quick` and `tests/release/app-release-boundary-cases/runtime-page-evidence-boundary.ts` fail closed if queued/escalated project lines lose `status`, `active_run_id`, `next_visible_step`, are presented as active worker runs, if `worker_not_ready` / missing Search Attributes lose their OPL repair commands, if provider repair overrides `current_owner_delta`, or if artifact drilldown grows artifact body/domain readiness authority. | Page-state tests and GUI smoke show summary-first read model, active project line separation, provider readiness repair command display, artifact-native refs-only drilldown, lazy full detail, dry-run/execute controls, receipt/count refresh, and authority-boundary fields. |
| `claude_science_runtime_task_awareness` | Runtime global task awareness is landed across Framework/App/AionUI mainlines: Framework readback emits refs-only stage/progress/next_owner/artifact_or_blocker/review_receipt/action_receipt/workflow refs and dry-run action preview; App contracts/fixtures/validators/tests cover task awareness and current-task slices; AionUI Runtime renders task ref summaries without owning runtime truth; AionUI conversation UI renders the current-task inline status and right-side workspace/inspector evidence refs from the same current-task slice. | Completion gate is met for refs-only task-awareness UI and contracts. Release-ready, domain-ready, artifact-body, owner-receipt, and quality/export verdict claims remain separate owner/evidence lanes. |
| `active_shell_sync` | App root wrappers prepare App-owned payloads and call the external shell. Current local readback: `shells/aionui` points at `/Users/gaofeng/workspace/opl-aion-shell` at `5c84da8a0110650cb44cfab4c31389e4d4ff6c19`; default `npm run validate:active-shell -- --quick`, `npm run validate:shell-candidates`, and `npm run validate:shell-convergence` pass. The convergence readback is a structure gate only: it keeps `active_shell=aionui`, `foreground_alternative=hermes-codex`, `archived_technical_proofs=["agui-codex"]`, and all release/live/adoption claims false. The previous stale local checkout (`b38a955b8b9cee07347616a7b7cfa5cf43b6c246`, ahead 1 / behind 7 plus uncommitted updater work) was preserved outside the tracked App tree under ignored `tmp/active-shell-checkout-backups/aionui-stale-20260620T221844` instead of being reset or merged. | Product profile and release contracts are generated into the active shell before build/release, without merging shell history into App mainline. A stale local checkout is a sync task, not evidence that the App contract or AionUI mainline failed; release-ready, active-shell-adopted, packaged GUI acceptance, production-ready, real user-path, and Live Evidence claims remain separate evidence gates. |
| `local_data_lifecycle_issue_5` | Legacy updater cache cleanup is implemented for stale installer packages, including the retired `~/Library/Caches/aionui-updater` root. The broader conversation/runtime/logs growth problem now has contract, active shell service, IPC, Storage UI, and focused test coverage. | Storage panel inventory is visible; conversation cleanup requires archive receipt plus restore proof and explicit user confirmation; runtime cleanup protects current/rollback pointers and only prunes unreferenced roots after dry-run; log rotation removes only log files by bounded policy. Packaged App smoke, real user-directory E2E, release-ready, and owner acceptance remain separate evidence gates. |
| `agui_codex_archived_technical_proof` | `contracts/app-shell-candidates.json` keeps `agui-codex` as `archived_technical_proof`; `contracts/shell-adapters/agui-codex.json` can still explicitly select the linked `shells/agui-codex` external repo for replay. `scripts/validate-shell-candidates.ts` now reads the archived proof policy from `alternative_gui_policy`, so default candidate validation covers Hermes only and AGUI enters validation only through explicit candidate selection. This proves only historical AG-UI/CopilotKit technical learning and remains out of default validation. AGUI-specific React/CopilotKit renderer and WebUI smoke checks are scoped to explicit replay, not to Hermes or generic shell adoption. The remaining AGUI `candidate` wording is compatibility with App adapter and historical manifests, not current foreground status. | Do not update or improve AGUI unless the user explicitly requests AGUI work. Any replay must stay explicit, preserve release isolation, and avoid domain-ready, production-ready, clean-VM-ready, Full-release-ready, or active-shell-adopted claims. |
| `hermes_codex_candidate_gate` | `contracts/app-shell-candidates.json` declares `hermes-codex` as the only foreground alternative GUI candidate; `contracts/shell-adapters/hermes-codex.json` can explicitly select `shells/hermes` or the sibling `../opl-hermes-shell` checkout for technical verification. `docs/product/shell-alternatives/hermes-gui-adaptation-plan.md` now owns the three-phase Hermes route: Phase 1 compatibility firewall, Phase 2 OPL branded Codex experience, and Phase 3 full OPL product profile. Phase 1 classifies each Hermes Desktop frontend dependency as `implement`, `adapt`, `diagnostic_only`, or `hide_or_remove`; ordinary UI must not present full Hermes backend surfaces such as Profiles, Cron mutations, Skills/Toolsets, Messaging/Handoff, provider marketplace, update/restart controls, audio/media remote helpers, or raw MCP management as working OPL Codex App features. MAS/MAG/RCA are Codex Skill/Plugin entries exposed through composer slash shortcuts and Settings status/readback only; the shell must not restore GUI purpose-route readback or become domain/runtime truth. Current `2026-06-18` source/package evidence and `artifacts/hermes-candidate-tart-20260618T123825Z/summary.json` are technical verification evidence for the current foreground-alternative cohort only. Package/runtime/visual acceptance remains incomplete because AionUI-vs-Hermes visual screenshot parity has not been accepted, packaged GUI acceptance is separate from local live app-server smoke, and Hermes remains a foreground-alternative technical-verification lane rather than the default release shell. | Candidate validation must prove upstream Hermes feature baseline preservation where kept, OPL branding/icon, fixed Codex app-server route, renderer-safe profile/config bootstrap, gflabtoken-only model access, nonblocking startup flow, missing-key model-access route, Simplified Chinese/English i18n, source and packaged smoke, release isolation, and no release-ready / active-shell-adopted / production-ready claim from contract-only evidence. Phase 1 completion additionally requires ordinary navigation and command surfaces to hide unsupported full-Hermes features or label them diagnostic-only, and requires adapter gaps that affect ordinary chat to be implemented or explicitly non-misleading. Default shell adoption still requires a deliberate `contracts/app-shell-adapter.json` change plus App-owned page-state, first-run, product-profile, runtime-bridge, packaged smoke, WebUI-claim, and release gates. |
| `pilotdeck_reference_intake` | OpenBMB PilotDeck is a reference-only information-organization source. Its evaluated ref, license, reference value, OPL mapping, and forbidden reuse policy are owned by `contracts/app-shell-candidates.json#design_references`, while this plan only keeps the App product gap and intake boundary. The reusable ideas are optional lightweight workspace/session rail, nested session list, chat-first main pane, grouped Files, Skills, Routing, Memory, Always-On context, process traces, and compact composer density. It is AGPL-3.0 and owns its own gateway/runtime/memory/router/always-on state, so it is not an App source dependency or runtime authority. | OPL re-expresses the useful organization patterns in App-owned GUI inventory and selected shell work without making them first-screen panels or a full workbench first screen. The ordinary App path remains chat-first by default, while `contracts/app-shell-candidates.json#design_references` keeps PilotDeck as reference-only with no vendored code, no runtime authority transfer, and no candidate promotion without license and adapter gates. |
| `app_docker_webui_beginner_path` | Docker/WebUI beginner path is now a non-live App contract and guide path: installer entry, browser-only API key entry, configure-codex runtime proxy smoke, startup diagnostics, failure recovery, and ordinary user progress surfaces are machine-readable in `contracts/app-install-exposure-policy.json` and covered by focused release-boundary checks. | Remaining release proof is intentionally outside this active plan: clean Linux/Windows VM, existing Docker, old data-dir migration, GHCR receipt, and same-cohort user-path artifacts must come from release/evidence lanes before any release-ready claim. |
| `settings_control_plane_unification` | Settings/model/reasoning policy now has a single App-owned control-plane source. `contracts/app-settings-control-plane.json#model_reasoning_policy_source` binds Settings Access Model & Account to `app-product-profile#codex`, `gui.home.codex_model_display_options`, and `app-gui-product-contract#executor_policy`; shells are adapter/render hosts only. | Shell implementation and visual QA can still prove rendering parity, but they cannot become a second policy source or replace release/live/provider evidence. |

## Release Claim Boundary

This active plan does not maintain release cohort, clean-VM, packaged `.app` smoke, same-cohort user-path, real user-directory E2E, owner acceptance, or production-ready worklists. Those stay in release artifacts, evidence manifests, validators, workflows, updater metadata, App release records, and owner receipts. This plan may mention them only to preserve false-ready boundaries: App validation, shell tests, contract pass, updater metadata, or refs-only OPL ledger entries do not prove release-ready, active-shell-adopted, domain-ready, or family production-ready claims.

## Next-Round Agent Prompt

Objective:

- Govern the One Person Lab App product docs, functional structure, shell/contract boundaries, and release-evidence routing without moving runtime/provider/domain authority into the App repo.

Write scope:

- `docs/active/app-ideal-state-gap-plan.md`, `docs/status.md`, `docs/project.md`, `docs/architecture.md`, `docs/invariants.md`, `docs/decisions.md`, App contracts, release-boundary validators, testing docs, and focused App tests that affect App product boundary, first-run policy, page-state validation, Settings / Storage, shell-candidate policy, or evidence routing.

Live truth inputs:

- `AGENTS.md`, user-level `~/.codex/TASTE.md`, `README.md`, `docs/README.md`, `docs/status.md`, this plan, App contracts, release-boundary validators, release/test scripts, shell adapter contract, active shell validation outputs, focused shell tests for App-owned GUI behavior, and `npm run validate:agent-installation` output when install exposure is touched. Release evidence manifests are read only for routing and false-ready boundaries unless the task explicitly targets release evidence.
- OPL Framework runtime/App drilldown CLI JSON only as consumed input; it remains framework-owned truth.

Required actions:

- Keep App docs, contracts, release wrappers, evidence manifests, and page-state tests aligned with the App product boundary.
- GUI design work 必须基于 App-owned 定义栈：先读理想交互细则，再读
  Codex-to-OPL delta，再读 feature inventory，最后对照 contracts 和 page-state
  matrices。
- Keep App product truth purpose-first: App is the Codex wrapper and ordinary user entry for MAS/MAG/RCA/BookForge purpose work, while runtime truth, domain readiness, owner receipts, memory/artifact bodies, and family production readiness stay outside App ownership.
- Keep Runtime progress display bound to OPL shared progress projection classifications only; platform repair must stay an infrastructure repair signal, not a deliverable or manuscript/submission progress claim.
- Keep Runtime default display user-task-status first: running, active, queued, attention, project progress, next visible step, next owner, delta class, and blocker state before full evidence ledger detail or provider diagnostics.
- Keep Claude Science learning mapped to Runtime task awareness, not a new dashboard: Framework/domain produce refs, App defines contracts, AionUI renders thin current/global slices, and Temporal remains diagnostics only.
- Keep the independent agent installation contract aligned with App/Framework/domain ownership: MAS/MAG/RCA are plugin-packaged skill entries, BookForge and OMA are OPL-generated, optional live plugin roots must contain `.codex-plugin/plugin.json` plus `skills/<id>/SKILL.md`, and optional Codex skills root validation must fail closed on duplicate bare MAS/MAG/RCA/BookForge mirrors.
- Keep the App ordinary path aligned with the Codex App equivalent positioning: Codex CLI fixed executor, no home or ordinary-conversation backend/provider/permission selector, MAS/MAG/RCA/BookForge as built-in purpose entries, and model choice bounded by the App-owned product profile.
- Keep Codex CLI conversation output rendering aligned with native Codex behavior: ACP tool-call normalization must preserve newline-bearing `raw_output` / `stdout` / `stderr` content instead of collapsing command output into one visual line.
- Keep built-in assistant sends observable with a route receipt: `route_kind=builtin_capability`, `executor=codex_cli`, `assistant_id`, `assistant_short_name`, and `source=opl_app_home`.
- Keep ordinary Settings aligned with the App-owned navigation partition: Overview, Setup & Access, Capabilities, Maintenance & Updates, Data & Storage, Preferences, and Advanced, with About/Update/Theme as secondary surfaces. Treat overview/runtime/system/model/agent/assistants/skills-hub/tools/display/webui/pet as legacy or diagnostics routes that redirect to App-owned pages.
- Keep shell candidate work isolated in `contracts/app-shell-candidates.json`, `contracts/shell-adapters/<candidate>.json`, and linked `shells/<candidate>` external repos until adoption; default release wrappers continue to use `contracts/app-shell-adapter.json` unless `OPL_APP_SHELL_ADAPTER_CONTRACT` is explicitly set for a candidate build.
- Keep AG-UI/CopilotKit material archived as technical proof only. Do not update or improve AGUI unless the user explicitly requests AGUI work; explicit replay still uses `docs/history/shell-candidates/agui-codex-candidate-verification.md`, the AGUI adapter contract, and release-isolated smoke artifacts.
- Keep Hermes candidate docs aligned with `docs/product/shell-alternatives/hermes-gui-adaptation-plan.md` and `docs/product/shell-alternatives/hermes-first-run-flow.md`: the target is upstream-first Hermes Desktop customization shaped into a Codex App-like OPL wrapper, not an AionUI/AG-UI surface port and not a full Hermes backend clone. Phase 1 must maintain the compatibility firewall classification (`implement`, `adapt`, `diagnostic_only`, `hide_or_remove`) before a full-Hermes feature can appear on ordinary paths. Candidate verification must include explicit adapter selection, App-root Hermes candidate validation, shell bootstrap/gateway tests, model/settings/i18n focused UI tests, ordinary navigation/command-surface checks, shell candidate validation, source smoke, packaged `.app` smoke, first-run timing artifacts, VM smoke when requested, and release isolation.
- Use PilotDeck only as a design reference for information organization and navigation shape. Re-express useful ideas as OPL-owned lightweight workspace/session rail and right-side collapsible inspector requirements; do not copy AGPL source, adopt PilotDeck runtime authority, expose its provider/model/router controls, or make a full workbench first screen on the ordinary App path.
- Keep release cohort evidence out of this active gap list. When release work is explicitly requested, classify evidence in the release-owned manifests as present, missing, typed blocker, or not applicable; never promote missing evidence to release-ready proof.
- Route retired release workflow no-resurrection checks to `scripts/validate-release-boundary.ts`, `tests/release/app-release-boundary-cases/workflow-release-channels.ts`, and `docs/history/process/retired-surface-provenance.md`; this active plan should only carry release/evidence gaps that need the next App owner action.
- When OPL App/operator drilldown reports current cohort release/user-path refs, reflect only the false-ready boundary or route the result to release evidence manifests; do not convert it into release-ready, domain-ready, or production-ready authority in this active plan.
- Keep active shell intake explicit: App-owned product/release contract changes stay here, shell implementation changes stay in `opl-aion-shell`.

Non-goals:

- Do not own OPL runtime truth, provider implementation, action-route authority, domain truth, domain quality/export verdict, memory body, artifact body, artifact authority, owner receipt authority, or shell implementation history.
- Do not use App UI rendering, provider completion, updater metadata, or release artifact existence as proof of MAS/MAG/RCA/BookForge readiness or family production readiness.

Verification commands:

- Docs-only: `rtk git diff --check`, `rtk rg -n "^(<<<<<<<|=======|>>>>>>>)" docs`, `python3 /Users/gaofeng/workspace/opl-doc/scripts/opl_doc_doctor.py doctor /Users/gaofeng/workspace/one-person-lab-app --format json`.
- App contract/release boundary changes: `rtk npm run test:release-boundary`, `rtk npm run validate:agent-installation`, live `npm run validate:agent-installation -- --codex-skills-root ~/.codex/skills --agent-root mas=<path> --agent-root mag=<path> --agent-root rca=<path>` when plugin roots are available, `rtk node --experimental-strip-types scripts/validate-active-shell.ts --quick`, and the touched release/evidence validation script.
- AG-UI/CopilotKit explicit replay only when requested: `rtk npm run validate:shell-candidates -- --candidate agui-codex`, `rtk env OPL_APP_SHELL_ADAPTER_CONTRACT=contracts/shell-adapters/agui-codex.json node --experimental-strip-types scripts/validate-active-shell.ts --quick`, and shell/package smoke commands only when AGUI evidence is explicitly being replayed.
- Shell convergence structure readback: `rtk npm run validate:shell-convergence`.
- Hermes candidate contract/docs changes: `rtk npm run validate:shell-candidates -- --candidate hermes-codex`, `rtk node --experimental-strip-types scripts/validate-hermes-candidate.ts`, `rtk npm --prefix shells/hermes run typecheck`, focused `rtk npm --prefix shells/hermes run test:ui -- <touched UI tests>`, `rtk node --test shells/hermes/electron/opl-bootstrap-runner.test.cjs shells/hermes/electron/opl-codex-gateway.test.cjs`, `rtk npm --prefix shells/hermes run validate:candidate`, and, when packaging evidence is claimed, `rtk npm run validate:shell-candidates -- --candidate hermes-codex --run-candidate-commands`. If `shells/hermes` is absent in a developer checkout, the accepted fallback is the sibling `../opl-hermes-shell`.
- Shell implementation changes: focused `vitest` for Guid/home/conversation selectors, Settings modal, Guid Access shortcut, and route receipt, `bunx tsc --noEmit`, `bunx oxfmt --check` on touched shell files, and packaged GUI smoke with `--assistant-route-smoke` for release evidence.

Completion gate:

- The App active plan, status, project, architecture, invariants, decisions, contracts, and test docs agree on App ownership and non-ownership.
- Agent installation exposure has a single semantic path: MAS/MAG/RCA plugin-packaged skills plus direct skill compatibility, no duplicate bare skill mirrors, and OMA as an OPL-generated skill surface.
- Home, ordinary conversation, ordinary Settings, and built-in assistant route receipt behavior are enforced by App contracts, active-shell validation, and active shell tests.
- Release evidence gaps are explicit; packaged assistant route smoke must be present before claiming packaged GUI Codex-path evidence, and no App doc claims runtime truth, domain ready, release ready, or production ready beyond the available artifacts and contracts.

Foldback target:

- Durable product truth folds back to `docs/status.md`, `docs/project.md`, `docs/architecture.md`, `docs/invariants.md`, `docs/decisions.md`, or App contracts.
- Release proof, screenshots, VM logs, remote verification, and command traces stay in release artifacts, evidence manifests, CI logs, history docs, or commit history, not in active docs as execution logs.
