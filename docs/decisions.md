# One Person Lab App Decisions

Owner: `one-person-lab-app`
Purpose: `app_decisions`
State: `active_truth_with_history_notes`
Machine boundary: Human-readable decision record. Machine-readable truth lives in `contracts/`, source, release artifacts, updater metadata, test outputs, active shell validation, and OPL Framework CLI/read-model output consumed by the App.

## Current Decisions

| Decision | Current reading | Machine boundary |
| --- | --- | --- |
| App product truth is purpose-first | The App is a Codex wrapper for ordinary users to enter research, grant, presentation, and book work. It owns the App/product boundary and user path, while runtime, domain, memory, artifact, owner receipt, and production-readiness truth stay with OPL Framework and domain owners. | `contracts/app-product-profile.json#boundary`, `contracts/app-gui-product-contract.json#product_authority`, `docs/project.md`, and `docs/architecture.md`. |
| App repo owns GUI product truth | App-level page behavior, model-selection policy, onboarding policy, screenshots, release/user docs, and release gates are governed from this repo. | `contracts/app-gui-product-contract.json`, `contracts/app-product-profile.json`, `contracts/app-page-state-matrix.json`, `contracts/app-first-run-test-matrix.json`, release scripts, and App tests. |
| GUI mainline and alternatives are fixed | The App GUI mainline is the OPL-branded AionUI shell. `opl-native-workbench` is the foreground alternative candidate once `contracts/app-shell-candidates.json` selects it. It is an independent shell repo/checkout under `shells/opl-native-workbench` and is governed by `contracts/shell-adapters/opl-native-workbench.json`. Hermes Desktop / `hermes-codex` is a retained explicit candidate for validation, feature comparison, and candidate package builds; it is not cleanup waste and is not part of the default foreground validation scope. AGUI/CopilotKit work remains archived technical verification and is updated only when AGUI is explicitly requested. | `contracts/app-shell-adapter.json`, `contracts/app-shell-candidates.json#active_gui_mainline`, `contracts/app-shell-candidates.json#alternative_gui_policy`, `contracts/shell-adapters/opl-native-workbench.json`, `contracts/shell-adapters/hermes-codex.json`, `docs/product/gui/gui-shell-candidates.md`, and `package.json#scripts`. |
| GUI 定义栈由 App 拥有 | 理想交互细则、Codex-to-OPL 产品增量、GUI 能力清单和机器 contracts 构成一组定义栈。修改 shell 行为前，GUI implementation work 应按这个顺序阅读。 | `docs/product/gui/ideal-interaction-spec.md`, `docs/product/gui/codex-to-opl-app-delta.md`, `docs/product/gui/feature-inventory.md`, and `contracts/`. |
| Ordinary App executor is fixed to Codex CLI | The App home and ordinary conversation paths are a Codex App equivalent wrapper with configurable professional-agent shortcuts over installed packages. Aion CLI, Claude Code, generic backend/provider switching, and permission-mode controls are not normal user choices; model control stays in the App-owned Codex model selector/status. | `contracts/app-gui-product-contract.json#executor_policy`, `contracts/app-product-profile.json#gui.home`, `contracts/app-page-state-matrix.json#guid_home`, `docs/product/gui/agent-package-management-plan.md`, active-shell validation, and shell GUI tests. |
| Model selector is visible and defaults strongest | Home and ordinary Codex conversations show the App default model as `5.6 Sol` through an App-owned model selector. Auto resolves to `gpt-5.6-sol` with `ultra` reasoning while the compact button shows `5.6 Sol 极高`. Outside Auto, the first-level selector exposes reasoning effort as the primary choice and routes fixed model selection plus OPL Flow 智力增强 on/off into secondary menus. Ordinary visible models follow the Codex App order and names: `5.6 Sol`, `5.5`, `5.6 Terra`, `5.6 Luna`, `5.4`, `5.4 Mini`, `5.3 Codex Spark`; other retired models stay hidden. Backend, executor, and permission selectors stay out of the ordinary path. | `contracts/app-product-profile.json#gui.home.codex_model_display_options`, `contracts/app-gui-product-contract.json#executor_policy`, `contracts/app-page-state-matrix.json#guid_home`, `shells/aionui/.../oplProductProfile`, `GuidPage`, `AcpModelSelector`, `AcpSendBox`, and focused shell tests. |
| Settings/model/reasoning policy is single-sourced by App contracts | Settings Access / Model & Account, Home, ordinary Codex conversations, AionUI, `opl-native-workbench`, Hermes prior-candidate surfaces, and WebUI must derive default model, visible model list, reasoning effort options, OPL Flow 智力增强 default-on policy, and access-readiness presentation from the App product profile plus GUI executor policy. Shells render/adapt this policy only and must not fork a provider/model/reasoning/intelligence-enhancement policy or promote release/live evidence into Settings completion. | `contracts/app-settings-control-plane.json#model_reasoning_policy_source`, `contracts/app-product-profile.json#codex`, `contracts/app-product-profile.json#gui.home.codex_model_display_options`, `contracts/app-gui-product-contract.json#executor_policy`, active-shell validation, and focused release-boundary tests. |
| Conversation pending feedback shows elapsed seconds | Sending a message must immediately expose visible processing feedback with elapsed seconds while ACP/Codex is pending or running, even if thinking/tool events have begun. Raw trace remains diagnostic-only. | `contracts/app-gui-product-contract.json#pages.guid_home.conversation_feedback_policy`, `contracts/app-page-state-matrix.json#guid_home`, `ThoughtDisplay`, `AcpSendBox`, and focused shell tests. |
| First GUID message warms ACP before send | Fresh installs can spend their first ACP startup window resolving the Codex ACP adapter and local runtime state. GUID-created initial messages must await `conversation.warmup` before posting `/messages`, so cold startup happens on the explicit warmup path instead of racing the first user message into the 30s ACP handshake timeout. | Active shell `useAcpInitialMessage`, `warmupConversation`, shell focused tests, and `scripts/validate-active-shell.ts`. |
| Professional agents are packages, not App modules | First-party starter Agent Registry entries and starter shortcuts run over the fixed Codex executor. OPL App should discover any compliant first-party, organization, user, or third-party package through a configurable GitHub/URL Registry, then route the selected manifest URL to Framework-owned validation, package lock, action receipts, exposure preference state, Home shortcut preferences, status, list readback, and manifest-declared local Codex plugin materialization. In Settings, Capabilities is package-directory-first: installed package rows, inline Home shortcut management, multi-axis status, and detail disclosure; purpose is secondary metadata only. Canonical runtime projection is now `app_state.agent_packages.directory + app_state.agent_packages.status_index`; the shell may use `app_state.modules.items[]` only as rollout fallback and must preserve developer-checkout semantics instead of collapsing them into a single repair state. Non-live first-party distribution payload proof, locked required skill-pack refs, remote payload manifest refs, uninstall cleanup contracts, and `rollback_ref` recovery refs are landed; installed Codex-surface reload proof and live install readiness remain separate runtime or release-owner evidence. Agent Package rollback is displayed as `rollback_ref`, fail-closed state, and repair/update route, not as a new App- or Framework-owned package lifecycle verb; managed update rollback remains owned by the Managed Update plane/runtime substrate/package-channel owner. Starter entries are defaults, not the App's capability ceiling, and the Registry does not define agent business behavior. Naming is layered and explicit: canonical machine ids for App package, Codex-visible entry, plugin id, and required Codex skill id all follow repo slugs (`med-autoscience`, `med-autogrant`, `redcube-ai`, `opl-bookforge`, `opl-meta-agent`); visible `short_name` values stay uppercase acronyms (`MAS`, `MAG`, `RCA`, `OBF`, `OMA`); legacy shorthand input is normalized back to the canonical package slug. Runtime, task, and package display/action mapping is single-sourced by `contracts/app-runtime-bridge.json#canonical_state_display_action_map`: AionUI and OPL Native Workbench may label and lay out the same semantics differently, but both consume App/root state and actual App action refs without inventing fallback currentness, readiness, domain truth, lifecycle execution, rollback semantics, or mutation authority. | `docs/product/gui/agent-package-management-plan.md`, `contracts/app-runtime-bridge.json#canonical_state_display_action_map`, `contracts/app-gui-product-contract.json#framework_surfaces.canonical_state_display_action_map`, `contracts/app-page-state-matrix.json#canonical_state_display_action_map_ref`, `contracts/agent-package-registry.json`, `contracts/agent-package-surfaces.schema.json`, `contracts/fixtures/agent-package-manifests`, `contracts/app-install-exposure-policy.json#agent_installation_contract.agent_registry_policy`, `contracts/app-product-profile.json#companion_payloads`, OPL Framework package receipts including `physical_surface`, Framework Home shortcut preference readback, and active-shell validation. |
| Source materials are Framework refs before domain agent work | When a user drops a PDF or file and states a goal, the App route is to call Framework-owned `opl workspace source ingest` and carry `source_material_refs`, `source_material_receipt_refs`, provenance refs, and later `reference_design_packet_refs` through the conversation/task readback. OMA may consume those refs for reference design distillation; MAS, MAG, RCA, and OBF consume source refs through their own domain authority. The App only shows user-friendly file/source/provenance status and passes refs; it does not parse PDF bodies, judge extraction quality, write domain truth, create owner receipts, or claim release/domain readiness. This is App-to-Framework route contract landing, not live drag-and-drop UI evidence. | `contracts/app-runtime-bridge.json#source_material_projection`, `contracts/app-gui-product-contract.json#source_material_user_path`, `contracts/app-page-state-matrix.json#ordinary_conversation`, and focused release-boundary validation. |
| Home shortcuts carry package/profile metadata, not a session contract | The ordinary Home composer should render configured shortcuts over installed OPL Packages that are meant to launch user work. In Settings Capabilities, shortcut visibility and order are managed inline on the package directory row rather than on a detached second table. Shortcut metadata may declare label, package id, required Codex skills, source, and refs-only display policy. It must not define domain workflow, MAS stage behavior, artifact schema, prompt internals, readiness verdict, or quality/export authority. OPL Flow is an OPL Package with `package_kind=workflow_plugin_package`, not a Home professional-agent shortcut. Existing `assistant_skill_profiles` are migration inputs toward generic package shortcut metadata. | `docs/product/gui/agent-package-management-plan.md`, `contracts/app-gui-product-contract.json#assistant_skill_profiles`, `contracts/app-product-profile.json#gui.assistant_skill_profiles`, `contracts/app-page-state-matrix.json#guid_home`, active-shell validation, and shell GUI tests. |
| Ordinary skill/MCP selection is OPL allowlist only | The ordinary Home composer and ordinary conversation loaded-capability surfaces use App-owned allowlists derived from the active package shortcut and App packaged skill policy. AionUI implementation skills such as `aionui-skills`, `aionui-webui-setup`, `skill-creator`, `cron` when not explicitly allowed by the active package, unlisted MCP servers, and disabled Team MCP snapshots such as `aionui-team`, `team_*`, and `mcp__aionui-team*` are excluded from ordinary selection, loaded-capability display, new conversation create payloads, and inherited session MCP state. | `contracts/app-product-profile.json#gui.ordinary_capability_selector_policy`, `contracts/app-gui-product-contract.json#ordinary_capability_selector_policy`, `contracts/app-page-state-matrix.json#guid_home`, active-shell validation, `GuidPage`, `useGuidSend`, `FileAttachButton`, `ConversationSkillsIndicator`, and shell whitelist tests. |
| Professional agent launch must be observable as an invocation receipt | Selecting a configured package shortcut creates a Codex CLI conversation and persists a launch-only receipt with executor, package/profile or assistant id, required skill ids, source, and launch surface. The active shell validates `opl_agent_package_invocation` in packaged route smoke while preserving the legacy route alias during migration. The receipt proves what the App launched; it does not govern session behavior or prove domain readiness. | `contracts/app-gui-product-contract.json#builtin_assistant_route_receipt_policy`, `contracts/app-product-profile.json#gui.builtin_assistant_route_receipt_policy`, `contracts/app-page-state-matrix.json#guid_home`, `docs/product/gui/agent-package-management-plan.md`, active-shell validation, and shell unit tests. |
| Ordinary Settings navigation 由 App 拥有 | Settings is the App OPL Control Center. The target ordinary IA groups pages by user question, scope, and risk as 概览, 访问方式, 工作目录, 智能体与能力, 资源与连接, 维护, 存储, and 偏好; Advanced, About, Update, Theme, and Local Services are secondary/deep-link surfaces. Resources & Connections is the ordinary route for Docker WebUI, OPL Workspace, managed resources, SSH/HPC, Fabric, and Console-managed refs, while Access stays focused on OPL Gateway, Codex CLI, and browser access to this computer. User-needed capabilities must be exposed as task entries such as Model & Account, Workspace, Maintenance Hub, Capability Status, Web / Remote Access, Resources & Connections, Developer Profile Status, External Tools & Voice, and Custom Assistant rather than as AionUI technical tabs. Capabilities itself is an installed package directory with integrated Home shortcut management, multi-axis state, and detail disclosure; purpose is a tag/filter, not the primary card identity. The ordinary layer shows user-comprehensible state, facts, and next steps; English navigation, raw enums, Docker/dry-run/payload mechanics, empty next-step cards, narrow maintenance columns, long theme/topic lists, root paths, refs, manifests, and hashes stay out of default content. Legacy upstream settings routes remain compatibility redirects and must not become ordinary navigation. `contracts/app-settings-control-plane.json` hydrates the registry, ordinary and secondary route behavior, legacy redirects, extension anchor remaps, state/action source policy, `SettingsHost` / `SettingsShellAdapterSlot`, required Access/Workspace/Environment/Storage/Capabilities view-model adapter entries, visual QA evidence policy, and `product_system_checklist` completion-audit source; `settings_ia.v1` remains the App GUI source contract. Advanced/About/Update/Theme/Local Services remain secondary or deep-link routes unless contract, matrix, validators, tests, and visual QA targets are changed together. Visual QA must cover the eight ordinary routes, bind desktop/mobile screenshots to a manifest, and keep secondary-route evidence explicit; it is shell behavior evidence and does not prove release readiness, packaged App readiness, runtime currentness, or owner acceptance. `product_system_checklist` keeps installed/release currentness as an explicit release-owner-gated audit item so Settings completion cannot silently omit it or infer it from UI tests. | `docs/product/gui/settings-control-center.md`, `contracts/app-settings-control-plane.json`, `contracts/app-shell-adapter.json#implementation_probes.settings_control_plane_shell_adapter_slot`, `contracts/app-gui-product-contract.json#settings_navigation.settings_ia`, `contracts/app-page-state-matrix.json#pages`, `contracts/app-product-profile.json#settings`, active-shell validation, release-boundary tests, shell Settings/Guid tests, and active-shell Settings E2E screenshot manifest evidence. |
| AionUI Settings fork maintenance uses the existing control plane | Claude's broad fork-maintenance proposal is narrowed to the current repo architecture: enhance the App-owned Settings Control Plane, keep `SettingsHost` / `SettingsShellAdapterSlot` as a thin shell adapter, and require upstream intake to be `accepted`, `adapt`, `redirect`, or `reject` before any registry or slot change. The App does not create a new integration package, Settings plugin ecosystem, overlay topology, or shell-owned product IA to solve fork maintenance; numeric ROI claims are excluded unless backed by fresh evidence. Shell delta remains renderer, bridge, route/slot, styling/i18n, and focused test evidence only. | `docs/architecture/opl-aionui-fork-maintenance-strategy.md`, `docs/product/gui/settings-control-center.md`, `contracts/app-settings-control-plane.json`, `contracts/app-gui-product-contract.json#product_authority`, and `contracts/app-shell-adapter.json#implementation_probes.settings_control_plane_shell_adapter_slot`. |
| Home minimal command surface and Settings IA require matrix parity | Home is composer-first and does not show runtime activity, continue-work, per-assistant running badges, or footer quick icons. Runtime/secondary context refs plus General/Access/Capabilities/Resources/Local Environment/Advanced sections must be declared in both the GUI product contract and page-state matrix. | `contracts/app-gui-product-contract.json#pages`, `contracts/app-page-state-matrix.json#pages`, `scripts/validate-active-shell.ts`, and focused release-boundary GUI tests. |
| Skill is the public ABI; plugin is the App distribution shell | Professional agents remain domain skills/plugins backed by their own action/stage metadata source. Codex App can expose them through plugin-packaged skills, while CLI/direct Codex paths use the same skill semantics. App install policy must not turn plugin-packaged domain skills into duplicate companion skill mirrors or App-owned session behavior contracts. | `contracts/app-install-exposure-policy.json`, `contracts/app-product-profile.json#companion_payloads`, OPL Framework `opl connect sync-skills`, `docs/product/gui/agent-package-management-plan.md`, and active-shell validation. |
| Codex configuration syncs App-visible OPL family plugins | Fresh Full installs must not require a second manual Codex plugin setup after the API key is configured. `opl system configure-codex --api-key-stdin` consumes the Full runtime module paths, syncs MAS/MAG/RCA plugin-packaged skills, generates the OMA and BookForge local plugin surfaces, and writes the Codex plugin registry in the same setup step. | OPL Framework `system configure-codex`, `syncFamilySkillPacks`, App Full runtime wrappers, and release-boundary wrapper validation. |
| Codex fallback uses an offline archive wrapper | The Full DMG remains self-contained on a clean machine without carrying the expanded Codex vendor binary directly in `runtime/current/bin`. The packaged fallback is `runtime/current/bin/codex` as a shell wrapper plus `runtime/current/vendor/codex/codex_cli_darwin_arm64.tar.gz`; `--version` stays fast and real Codex execution extracts the archive into the installed runtime cache before exec. | `contracts/app-release-channel.json#full_first_install.required_payloads.codex_cli`, `scripts/build-full-first-install-package/runtime-layers.ts`, `scripts/verify-remote-release-assets.ts`, and Full clean-VM smoke evidence. |
| First conversation waits for ACP warmup, not Full readiness | First-run Core readiness still requires only workspace root, Codex CLI, and usable Codex model access before `/guid`. OPL Gateway is the managed/recommended path, but an existing Codex login or other usable provider can satisfy first-launch readiness without forcing Gateway configuration. The first `/guid` message must wait for the conversation record and ACP warmup before `sendMessage`, so slow first-run dependency unpacking does not become a fixed 30-second ACP handshake failure. Domain modules, Codex plugin sync, and Temporal provider readiness stay visible background maintenance. | `contracts/app-install-exposure-policy.json#setup_flow_contract.first_conversation_readiness`, `contracts/app-product-profile.json#first_run.first_conversation`, active-shell `useAcpInitialMessage`, and shell unit tests. |
| First-run preparation is visible but skippable after Codex core is available | A first-run checklist may carry real OPL preparation tasks because it gives users visible progress, but non-core/network-dependent preparation must not trap users before the App. Once Codex CLI / Codex adapter can start and any usable Codex model access exists, the user can skip OPL Gateway first-launch configuration into the chat-first shell; the skip writes a `user_deferred` routing marker and must not claim OPL Gateway API key configured, module reconcile complete, MAS/MAG/RCA domain ready, or Full OPL readiness. Settings must keep an OPL Gateway configuration entry so users can switch later. AionUI and foreground candidates may adopt this pattern only through their adapter contract and release-shell validation. | `docs/product/gui/opl-native-workbench-plan.md`, `docs/product/shell-alternatives/hermes-first-run-flow.md`, `contracts/shell-adapters/opl-native-workbench.json`, candidate first-run contracts, and focused candidate tests. |
| Temporal defaults are App-configured and OPL-owned | The App may configure default Temporal environment for the packaged OPL runtime, but it does not own provider implementation or runtime truth. Full runtime wrappers export `OPL_FAMILY_RUNTIME_PROVIDER=temporal`, `OPL_TEMPORAL_ADDRESS=127.0.0.1:7233`, `OPL_TEMPORAL_NAMESPACE=default`, and `OPL_TEMPORAL_TASK_QUEUE=opl-stage-attempts`; the bundled Temporal CLI fallback is an offline archive wrapper over `runtime/current/vendor/temporal/temporal_cli_darwin_arm64.tar.gz`, extracting into the installed runtime cache only when Temporal is actually executed. OPL Framework owns service start, worker status/start, residency proof, lifecycle diagnostics, and repair receipts. | `contracts/app-install-exposure-policy.json#temporal_auto_configuration`, `contracts/app-release-channel.json#full_first_install.required_payloads.temporal_runtime_provider`, `scripts/full-first-install-runtime-wrappers.ts`, `scripts/verify-full-runtime-native-trust.ts`, and OPL Framework family-runtime commands. |
| Install/update taxonomy has seven user-facing layers | App maintenance is explained as Installation Carrier, OPL Runtime Payload / Fabric, OPL Packages, Companion Tools, Codex Surface, Workflow Profile, and User Data/Artifacts. `runtime_substrate` remains the machine id, but user-facing docs and Settings use OPL Runtime Payload / Fabric. Full first-install assets preload payloads for clean machines; they are not a long-term update channel. The standard updater is macOS App carrier only; Docker/WebUI and Linux carrier updates stay host-route. Runtime Payload follows stable/nightly/release-host routing and never borrows the OPL Package rolling `latest` channel. `embedded_codex_executor` means the App-owned Codex CLI executor payload and must not mutate a user's global Codex/Homebrew/system install. Background maintenance may automatically apply OPL Package updates only for clean managed roots after Framework check/plan gates pass and receipts can be written; dirty/developer/manual-required states fail closed, and repair/rollback/destructive changes stay explicit user or release-owner actions through Framework receipts. MAS/MAG/RCA/OMA/BookForge/MAS Scholar Skills are domain or framework capability packages; OPL Flow is the same managed package class with `package_kind=workflow_plugin_package`. Workflow Profile material is only `AGENTS.md`, `TASTE.md`, and prompts; existing profiles route through Codex semantic merge packets and are not silently overwritten. | `contracts/app-release-channel.json#standard_updater`, `contracts/app-release-channel.json#managed_update_plane`, `contracts/app-install-exposure-policy.json#runtime_substrate_auto_update`, `contracts/app-install-exposure-policy.json#agent_installation_contract`, `docs/architecture.md`, and `docs/delivery/release/README.md#install-and-update-taxonomy`. |
| OPL Runtime Payload / Fabric is the shared runtime foundation, not a capability module | Ordinary App users get quiet maintenance for App-owned runtime roots grouped as Agent Execution Core (`embedded_codex_executor`, Temporal archive, OPL Framework runtime), Environment Materializer (Node/Python, uv, managed env cache, isolated prefixes, and receipts), and OPL System Bridge (native helper). MAS/MAG/RCA/OMA/BookForge/MAS Scholar Skills/OPL Flow package payloads are OPL Packages, not OPL Runtime Payload / Fabric. Companion tools such as OfficeCLI and MinerU may share the managed updater kernel but remain a separate class. OPL Runtime Payload maintenance must not write Electron `latest*.yml`, make Full assets updater-visible, consume GHCR OPL Package `latest`, or silently mutate user global Homebrew/system Codex or Temporal installs. System PATH, Homebrew, global tools, and developer checkouts are diagnostic or explicit expert opt-in unmanaged sources, not ordinary defaults. Pixi is only an optional resolver slot for modules that explicitly declare scientific/native stack needs; it is not part of Standard defaults. | `contracts/app-release-channel.json#runtime_substrate_updater`, `contracts/app-release-channel.json#managed_update_plane.planes[runtime_substrate]`, `scripts/validate-release-boundary.ts`, `scripts/validate-active-shell/release-contract-validator.ts`, release-boundary behavior tests, `docs/delivery/release/README.md#homebrew-distribution-boundary`, and OPL Framework `opl update apply/repair/rollback --json` runner outputs. |
| OPL Framework artifact evidence is an App release gate | The OPL Framework runtime artifact is part of OPL Runtime Payload / Fabric release readiness. A candidate must prove the Framework artifact channel, artifact readback, sha256 checksum, and rollback ref/receipt before App release gates can pass. The App consumes refs/checksums/status only and must not read artifact bodies, implement the Framework update kernel, or treat this as permission to update a Docker/WebUI image from inside Docker. Docker/WebUI image replacement stays an Installation Carrier host route. | `contracts/app-release-channel.json#runtime_substrate_updater.framework_artifact_gate`, `contracts/app-release-channel.json#managed_update_plane.release_boundary_required_cases`, `scripts/validate-active-shell/managed-update-plane-validator.ts`, `scripts/validate-release-boundary.ts`, release-boundary behavior tests, and OPL Framework artifact receipts consumed through `opl update status/check/plan/apply/repair/rollback --json`. |
| Runtime/resource/task/data lifecycle refs use one owner matrix | OPL Runtime Payload / Fabric, Environment Materializer, TaskRunProjection v2, OPL Fabric resource refs, local data lifecycle, Aion shell rendering, and Homebrew distribution are kept in one matrix so new refs do not become competing state machines. App owns user-facing policy and release gates, OPL Framework owns family projections and runtime receipts, Aion renders as a thin consumer, and Homebrew mirrors release cohorts only. | `contracts/app-runtime-bridge.json#runtime_surface_owner_matrix`, `contracts/app-runtime-bridge.json#task_awareness_projection`, `contracts/app-release-channel.json#local_data_lifecycle`, active-shell validation, and release-boundary tests. |
| GHCR OPL Packages are daily rolling latest artifacts | MAS/MAG/RCA/OMA/BookForge/MAS Scholar Skills and OPL Flow consume managed OPL Package artifacts, not Git checkouts, for ordinary users when release-published. `latest` is the ordinary user OPL Package channel: daily CI builds a candidate only when source changed, runs package/manifest/Codex Surface gates, then promotes that candidate to `latest`. Immutable version tags and resolved OCI digests are the installed truth; `latest` is only the moving discovery/update pointer. Managed package roots update silently only when the Framework reports clean managed eligibility and must report component receipts plus post-apply sync status for Codex plugin registry, plugin-packaged skills, generated OMA/BookForge surfaces, MAS Scholar Skills package surface, OPL Flow package surface, Codex Surface readiness, and App/Codex plugin-cache reload guidance. Developer checkouts are explicitly excluded from auto-apply. | `contracts/app-install-exposure-policy.json#agent_installation_contract.managed_agent_pack_distribution`, `contracts/app-release-channel.json#homebrew_tap_distribution.agent_pack_policy`, `contracts/app-release-channel.json#managed_update_plane.planes[capability_packages]`, OPL Framework `module-package-channel.ts`, `startup-maintenance.ts`, and package-channel/startup-maintenance tests. |
| Local Environment owns the ordinary OPL Packages maintenance entry | The App does not add a new top-level Settings tab for package maintenance. Local Environment carries the user-friendly MAS/MAG/RCA/OMA/BookForge/MAS Scholar Skills/OPL Flow OPL Packages status, Codex Surface readiness substatus, recommended action, and manual check/apply/repair/rollback mappings for OPL Packages. Updates & Maintenance keeps the advanced unified detail view: managed runtime/package/companion components use `opl update status/check/apply/repair/rollback` or App state/action routes, while installation carrier updates use carrier-specific host update route refs; it must not implement the update kernel, silently mutate developer or dirty checkouts, treat Codex Surface as a second package channel, or write runtime/domain truth. | `contracts/app-gui-product-contract.json#pages.settings_environment.module_maintenance_entry`, `contracts/app-page-state-matrix.json#environment`, `scripts/validate-active-shell/gui-product-contract-validator.ts`, and `scripts/validate-active-shell/page-state-app-settings-validator.ts`. |
| Local data cleanup is inventory-first and artifact-safe | Updater installer packages, conversation artifacts, OPL Runtime Fabric roots, and logs are separate lifecycle classes. The App may clean stale updater installer cache packages, but conversation workdirs and generated deliverables require inventory, archive/export, restore proof, and explicit user confirmation before deletion. OPL Runtime Fabric prune follows current/rollback pointers and starts as dry-run; logs use bounded rotation and cannot stand in for artifact cleanup evidence. | `contracts/app-release-channel.json#local_data_lifecycle`, active-shell validation, `shells/aionui/packages/desktop/src/process/services/localDataLifecycle/index.ts`, focused shell lifecycle tests, and historical Issue #5 provenance in `docs/history/process/local-data-lifecycle-issue-5.md`. |
| OPL Packages channel failures fail closed with packaged fallbacks | A missing or unavailable GitHub Packages/GHCR OCI OPL Package `latest` tag or digest is a package-channel failure, not a reason to hand-install a divergent OMA, MAS Scholar Skills, or OPL Flow surface. The source order is bundled Full runtime modules, App/CLI-managed GHCR OPL Packages `latest` channel, then explicit Developer Profile checkout source. Git repo or local checkout sources are development inputs only; they are surfaced as Developer Profile state and are never ordinary managed install/update state. Failures surface as actionable background-maintenance errors while preserving one Codex-visible plugin/skill semantic path. | `contracts/app-install-exposure-policy.json#agent_installation_contract.managed_agent_pack_distribution`, `npm run validate:agent-installation`, Full runtime module path wrappers, and OPL Framework `opl connect reconcile-modules` / `opl connect sync-skills`. |
| App packaged skills use one whitelist | `default_packaged_codex_skill_ids` is the only App product default for skills that are both packaged and default visible. Candidate source is intentionally ignored: AionUI builtin, Skills Manager, Codex local skill, and plugin-packaged skill are all sources to copy from, not separate product policies. `packaged_not_default_visible_codex_skill_ids` is reserved for explicit-only packaged skills such as OMA. | `contracts/app-product-profile.json#companion_payloads`, Full first-install package scripts, full package manifest, and active-shell validation. |
| Settings builtin skills are filtered through the App whitelist | The active shell may report upstream builtin skills and builtin-auto skills, but ordinary Capabilities only displays builtin catalog skills from the App packaged skill set and auto-injected skills from `default_packaged_codex_skill_ids`. AionUI helper skills such as `aionui-skills` do not surface as OPL App capabilities. | `contracts/app-gui-product-contract.json#pages.settings_capabilities`, `contracts/app-page-state-matrix.json#capabilities`, `contracts/app-product-profile.json#companion_payloads.default_packaged_codex_skill_ids`, `SkillsHubSettings`, active-shell validation, and shell tests. |
| Active shell stays an external implementation carrier | `shells/aionui/` is the current active shell checkout from `gaofeng21cn/opl-aion-shell`; shell history and implementation truth stay in the shell repo. The upstream AionUI fork body is read-only by default: App work may touch only App contracts, adapters, OPL overlays, packaging/readback hooks, and validation for those surfaces. | `contracts/app-shell-adapter.json` and active-shell validation decide whether a shell satisfies the App contract. |
| Active shell upstream intake is classified before release | Broad AionUI intake uses fail-closed `absorbed`, `rejected`, or `deferred` records for every validator-owned required capability and dependency. The v2.1.31 intake distinguishes fork base `70974c59`, evaluated upstream `e49cd949`, and selective absorption head `e38b00ba`; startup directories, database recovery, feedback privacy, Cron, `/guid` allowlist, and Chinese/English Settings/i18n refinements are absorbed, while non-Chinese/English locales and Team are rejected. The validator reads the resolved active shell package, fixes the AionCore minimum at `v0.1.44`, and requires selective absorption and remediation refs to be ancestors of shell `HEAD` rather than permanent exact-HEAD pins. Recovery admission additionally requires verified boundary evidence and a remediation-bound shell commit; the final managed-retry AionCore remediation ancestor is `a5811dd39`. Settings-specific registry/slot intake separately keeps fixed `accepted`, `adapt`, `redirect`, or `reject` buckets. | `contracts/app-shell-adapter.json#upstream_intake`, `scripts/validate-active-shell/upstream-intake-policy-validator.ts`, `tests/release/app-release-boundary-cases/upstream-intake-policy-validation.ts`, `docs/architecture/opl-aionui-fork-maintenance-strategy.md`, `contracts/app-settings-control-plane.json#upstream_intake_classification`, and active-shell validation. |
| Runtime page is user task status first | The App consumes `opl app state --profile fast --json` for the default user task view and `opl runtime app-operator-drilldown --json` only as secondary runtime diagnostics. Its daily Runtime page first answers: how many tasks are running, how many projects/tasks are active, how many are queued, how many need attention, and where each task is now. Required user fields are `running_task_count`, `active_project_count`, `queued_project_count`, `attention_count`, plus per-task title, status, stage, progress label, next step, owner, and last progress. Temporal/provider/projection/ref/stage attempt/ledger/current_control_state terminology must not be default-visible. `running_provider_attempt_count` may include checkpointed provider refs and remains diagnostic; it must not be displayed as the user running task count. Full detail stays on demand, actions stay safe dry-run capable, and all task/project lines remain refs-only display projections. The App does not own runtime truth, provider implementation, domain truth, progress truth, memory body, artifact body, owner receipt authority, action route authority, or domain verdicts. | `contracts/app-runtime-bridge.json`, `contracts/app-page-state-matrix.json`, `contracts/app-gui-product-contract.json#pages.runtime_status`, `contracts/fixtures/opl-app-state-fast.fixture.json`, and OPL Framework CLI/read-model output. |
| App, Workspace, Fabric, Gateway, and Console have distinct product roles | OPL App is the local-first, cloud-continuous workbench: users keep the same project, task, artifact, progress, and receipt language on the macOS desktop App, Docker/WebUI in a browser, and hosted OPL Workspace. Docker/WebUI is the browser runtime form of the same App experience for Linux, Windows, servers, and cloud VMs; OPL Workspace is that WebUI hosted with account, storage, isolation, and managed-resource policy. OPL Gateway is the AI access surface. OPL Fabric provides refs-only resource context for OPL Connect, compute, storage, Environment Catalog templates, resource usage, and plan/approve/execute/monitor/collect receipts. OPL Console manages organization policy, quota, billing, permissions, Workspace lifecycle, connector approval, environment policy, and managed resource packages only for Console-managed resources. User-provided local, SSH, or HPC resources remain self-managed unless Framework projections mark them as Console-managed. | `contracts/app-runtime-bridge.json#task_awareness_projection.resource_context_policy`, `contracts/app-gui-product-contract.json#pages.settings_access.cloud_fabric_surface`, `contracts/app-settings-control-plane.json#slot_registry.settings_access`, `contracts/app-page-state-matrix.json#pages`, and `docs/architecture.md`. |
| Claude Science learning lands through Runtime task awareness | Claude Science is treated as an external workflow pattern source, not as a new App dashboard or shell-owned task system. Reusable lessons land by strengthening the existing Runtime global task projection and by showing the same projection's current-task slice in chat and the right inspector. OPL Framework/domain producers own task, artifact, reviewer, workflow, connector, and action receipt refs; the App repo owns display contracts and validation; AionUI only renders thin generic components from App state/profile and calls App action routes. | `docs/product/gui/claude-science-runtime-task-awareness-plan.md`, `contracts/app-runtime-bridge.json`, `contracts/app-page-state-matrix.json`, `contracts/app-gui-product-contract.json#pages.runtime_status`, OPL Framework `opl app state` / `opl app action`, and active-shell focused tests. |
| OpenScience accepted items are refs-only App projections | The four accepted OpenScience items are product projection boundaries: structured result panels live inside conversation/current-task/right inspector, artifact/provenance appears as refs-only card/drawer, ref-level comments become review/request-change/follow-up prompt/action refs without an App annotation store, and workflow/skill candidates are report-first Settings / Capabilities refs with review, needs changes, and continue in conversation actions. OPL App remains the shell-wrapped Codex App, and professional agents are Codex plugins or packaged Codex skill surfaces. | `contracts/app-runtime-bridge.json#structured_result_panel_projection`, `contracts/app-runtime-bridge.json#artifact_provenance_bundle_projection`, `contracts/app-runtime-bridge.json#ref_level_follow_up_projection`, `contracts/app-runtime-bridge.json#workflow_skill_candidate_projection`, `contracts/app-page-state-matrix.json`, `contracts/app-gui-product-contract.json`, `contracts/fixtures/opl-app-state-fast.fixture.json`, and focused release-boundary tests. |
| Provider readiness repair stays secondary and actionable | When App state exposes provider readiness blockers, the Runtime page must give an explicit OPL repair command: `worker_not_ready` points to `opl family-runtime worker start --provider temporal`, while missing Temporal Search Attributes point to `opl family-runtime provider repair --provider temporal`. These are provider liveness/visibility repair paths only. They must not claim domain readiness, App release readiness, family production readiness, owner receipt authority, typed blocker authority, or override `current_owner_delta` as the default owner-action payload. | `contracts/app-runtime-bridge.json#provider_readiness_repair_projection`, `contracts/app-page-state-matrix.json#runtime.runtime_view_model.provider_readiness_repair`, `contracts/app-gui-product-contract.json#framework_surfaces.provider_readiness_repair`, `scripts/validate-active-shell.ts`, and `tests/release/app-release-boundary-cases/runtime-page-evidence-boundary.ts`. |
| Runtime State Index sidecar is read-model only | The App consumes OPL State Index Kernel / SQLite sidecar output only as Framework-projected read-model refs. The allowed path is App state JSON, explicit full App state, or task operator drilldown JSON; refs can drill down to Stage Folder locations. The App cannot directly read or write SQLite sidecar files, mutate the State Index Kernel, write domain truth, create owner receipts, read artifact bodies, or authorize readiness, artifact authority, quality/export verdicts, App release readiness, or family production readiness. | `contracts/app-runtime-bridge.json#state_index_sidecar_projection`, `contracts/app-page-state-matrix.json#runtime.runtime_view_model.state_index_sidecar`, `contracts/app-gui-product-contract.json#pages.runtime_status.state_index_sidecar_policy`, `contracts/fixtures/opl-app-state-fast.fixture.json`, and OPL Framework State Index Kernel projection output. |
| Runtime task drilldown consumes artifact-native refs only | The App consumes OPL Stage Artifact Kernel workbench refs for current pointer, canonical artifacts, export artifacts, lineage, retention, conformance, manifest/hash/receipt/blocker refs. This is a display/read-model contract only: the App does not read artifact bodies, own artifact authority, or declare MAS/MAG/RCA/OMA quality verdict, export readiness, domain readiness, App release readiness, or family production readiness. | `contracts/app-runtime-bridge.json#artifact_native_drilldown_projection`, `contracts/app-page-state-matrix.json#runtime.runtime_view_model.artifact_native_drilldown`, `contracts/app-gui-product-contract.json#pages.runtime_status.artifact_native_drilldown_policy`, `contracts/fixtures/opl-app-state-fast.fixture.json`, and OPL Framework `contracts/opl-framework/stage-artifact-runtime-contract.json`. |
| AionUI Team is hidden in ordinary OPL App | Upstream AionUI Team is configured around shell-local team leaders and agents, so it is not an OPL ordinary-user capability. The active shell keeps Team mode disabled, hides the Team sidebar entry, no-ops Team-created redirects, rejects Team deep links, redirects compatible `/team/*` routes back to the App-owned home path, and scrubs disabled Team MCP state from ordinary conversations before rendering or inheriting it. | `contracts/app-gui-product-contract.json#settings_navigation.team_surface_policy`, `contracts/app-gui-product-contract.json#ordinary_capability_selector_policy`, `contracts/app-page-state-matrix.json`, `scripts/validate-active-shell.ts`, and active shell `TEAM_MODE_ENABLED` plus ordinary conversation Team MCP scrub tests. |
| Release/user-path evidence is cohort-bound | App release and user-path evidence can support a same-cohort App release review, but draft bundles, verified refs, updater assets, screenshots, and packaged route smoke do not prove stable/latest promotion, MAS/MAG/RCA/BookForge readiness, or OPL family production readiness. | `contracts/app-release-channel.json#operator_evidence_bundle`, release evidence manifests, remote release verification, and release owner promotion records. |
| Developer Profile expresses capabilities, not one big switch | Ordinary users get OPL Package updates through Framework/App maintenance after the App is installed. Developer behavior is shown as a Developer Profile with explicit capability axes: `source_channel`, `workspace_trust`, `github_authority`, `agent_automation`, and `runtime_mutation_scope`. GitHub repo or local checkout module sources are an explicit `source_channel` opt-in, surfaced in Advanced/Local Environment from `app_state.developer_profile` and `app_state.modules[].source_policy`; raw `OPL_MODULE_SOURCE_MODE` remains a low-level diagnostic/CI override, not normal Settings UI. `opl-flow` is an OPL Package with `package_kind=workflow_plugin_package`; its Workflow Profile files still require semantic merge and remain outside standard updater assets and WebUI image publishing. | `contracts/app-gui-product-contract.json#developer_profile`, `contracts/app-gui-product-contract.json#module_path_source_policy`, `contracts/app-page-state-matrix.json#environment`, `contracts/app-product-profile.json#settings.developer_profile`, `contracts/app-first-run-test-matrix.json#updater_standard_channel`, and active-shell validation. |
| Homebrew is App cask transport and index only | Homebrew points terminal users and automation at the same App release cohorts through `one-person-lab`, `one-person-lab-nightly`, and explicit stable `one-person-lab-full` casks; it does not own App activation, user workspace state, module readiness, agent-pack distribution, skill/plugin semantics, updater policy, or stable/latest promotion. After a Homebrew install or upgrade, the activation source is still `opl system initialize --json`, with `opl install`, `opl system startup-maintenance`, `opl connect reconcile-modules`, and `opl connect sync-skills` handling user-state, modules, agent packs, and Codex-visible exposure. New stable desktop releases update the stable `one-person-lab` cask by direct tap commit only after the promote workflow publishes the draft release, then run the Homebrew VM gate against that published cask; existing published release refreshes may update the tap after remote asset verification and before the Homebrew VM gate. Nightly tap updates stay opt-in/prerelease and point only at standard macOS arm64 assets; Full tap updates stay stable-only and point only at `One-Person-Lab-Full-*` plus `full-package-manifest.json`. Tap freshness is primarily owned by the tap repo's self-sync workflow; same-owner App release tap writes use direct commits only and must not open tap pull requests. | `docs/delivery/release/README.md#homebrew-distribution-boundary`, `contracts/app-release-channel.json`, `contracts/app-install-exposure-policy.json#setup_flow_contract`, `contracts/app-install-exposure-policy.json#agent_installation_contract`, Homebrew tap workflow/check output, and OPL Framework `opl system initialize --json`. |
| WebUI uses the same chat-first workbench as desktop | WebUI is how the same OPL App workbench runs in a browser on Linux, Windows, servers, or cloud VMs. It must keep the same chat-first task flow, project/artifact/progress/receipt language, and OPL state/action boundary as the desktop App; it is not a second product profile, runtime source, provider selector, memory store, artifact store, release channel, or full workbench first screen. Current foreground candidate work should apply this through `opl-native-workbench` only when its WebUI delivery surface is explicitly claimed; Hermes WebUI remains prior-candidate reference material, and AGUI WebUI evidence remains archived technical proof. | `contracts/app-shell-candidates.json#alternative_gui_policy`, `docs/product/gui/feature-inventory.md`, `docs/product/gui/opl-native-workbench-plan.md`, and candidate-specific smoke evidence when explicitly claimed. |
| Docker/WebUI beginner path is a non-live functional path before release evidence | The beginner WebUI path is one-click installer first, browser Access API key entry, runtime proxy smoke through `opl system configure-codex --api-key-stdin --json`, startup diagnostics, data/project preservation, host-side image update route, and ordinary user progress after WebUI opens. Local and optional remote image digest comparison may show `current` or `update_available`, but it is status guidance only and does not prove release-ready, live latest/currentness, or applied host update. This closes the App contract/guide/validator path only; clean VM, GHCR receipt, live provider access, owner acceptance, and release-ready remain release-owner evidence. | `contracts/app-install-exposure-policy.json#installer_surfaces.docker_webui.installer_model`, `scripts/install-docker-webui.sh`, `scripts/install-docker-webui.ps1`, `scripts/docker-webui-smoke-gate.ts`, `scripts/validate-docker-webui-diagnostics.ts`, `docs/guides/docker-webui-install/guide.qmd`, and Docker/WebUI focused release-boundary tests. |
| Docker/WebUI cloud deployment fails closed on external secrets | Local beginner Docker/WebUI keeps browser auto-login and browser-first access-key entry. Server/cloud deployment is an explicit separate path: `deploy/docker-webui/cloud` uses Caddy/TLS, Docker secrets, default username `opl`, required WebUI password, optional Gateway API key overlay, and disabled auto-login. Any cloud/password mode or Gateway API key secret without a WebUI password secret must refuse startup; Gateway API key setup remains stdin-only through the local runtime proxy and does not replace WebUI login. This is a structural/security landing, not proof that a live cloud endpoint is deployed or release-ready. | `contracts/app-install-exposure-policy.json#installer_surfaces.docker_webui.installer_model.cloud_deployment_model`, `deploy/docker-webui/cloud`, `scripts/install-docker-webui.sh --cloud-template`, active shell `packages/web-cli/src/deploymentAuth.ts`, `resources/opl-webui-entrypoint.sh`, and focused Docker/WebUI tests. |
| Release efficiency target is build-once/promote-many | A stable release freezes one App/Shell/Framework cohort, builds and qualifies artifacts once, and promotes from a ready candidate record plus owner receipt. The release cohort manifest is the retry entrypoint for rerunning failed gates, VM diagnostics, or promote, not a new source of release truth. Public release notes are prepared as AI-written notes before publish/promote; publish/promote jobs must consume prepared files and must not call AI to generate notes. Full runtime bundle preparation belongs to OPL Framework and is consumed by the App as manifest/lock/readback refs. VM smoke qualifies the exact same-cohort artifact rather than rebuilding or creating missing release material. Target timing is standard 10-20 minutes, Full 35-50 minutes, same-cohort retry 3-15 minutes, and promote under 5 minutes. | `contracts/app-release-channel.json#release_acceleration`, `contracts/app-release-channel.json#public_release_notes`, `contracts/app-release-channel.json#full_first_install.opl_runtime_bundle_consumer`, `docs/delivery/release/README.md#release-efficiency-target-architecture`, `scripts/README.md`, release cohort plan/manifest artifacts, release readiness summary, candidate record, VM smoke artifacts, and promote workflow output. |
| Standard Stable readiness is decoupled from add-on gates by default | Standard Stable readiness admits from standard publish, standard remote verification, the standard VM gate, and one-shot installer smoke. Full, Docker/WebUI, and Homebrew remain same-cohort add-on gates/assets whose status is recorded in `release-addon-readiness-summary`, but they do not delay the Standard readiness record. | `.github/workflows/desktop-release.yml#release-readiness-admission`, `.github/workflows/desktop-release.yml#release-addon-readiness-summary`, `contracts/app-release-channel.json#release_acceleration.target_architecture`, `contracts/app-release-channel.json#release_acceleration.github_actions.release_readiness_admission`, and release readiness job results. |
| OPL native workbench candidate follows an App-owned structure route | `opl-native-workbench` is the foreground alternative candidate, not the active release shell. The first route is candidate registration, adapter contract, independent external checkout, state/action bridge, shared renderer, package manifest, source/WebUI smoke when claimed, docs/runbook, and later visual/live evidence. K-Dense, OpenClaudeScience / Claude Science, and AGUI lessons are classified as `adopt`, `adapt`, `watch_only`, or `reject`; external runtime/agent authority, Pi/DeepAgents/LangGraph-like runtimes, provider/backend marketplaces, and domain truth ownership are not App or shell authority. Hermes remains prior foreground alternative experience, not the default foreground scope. | `docs/product/gui/opl-native-workbench-plan.md`, `docs/product/gui/kdense-byok-learning-landing.md`, `docs/product/gui/claude-science-runtime-task-awareness-plan.md`, `docs/active/app-ideal-state-gap-plan.md#opl_native_workbench_candidate_gate`, `contracts/app-shell-candidates.json`, and `contracts/shell-adapters/opl-native-workbench.json`. |
| External UI projects can inform information organization without becoming authority | PilotDeck is a reference for optional workspace/session rail, chat-first main pane, grouped files, memory, routing, and always-on context. Its AGPL source and runtime are excluded unless a separate license and candidate-adoption decision is made, and it is not a first-screen workbench template. | `contracts/app-shell-candidates.json#design_references` and `docs/product/gui/feature-inventory.md#PilotDeck-Informed Information Organization`. |
| Standard updater and Full first-install assets stay separate | Standard updater metadata must never select `Full` assets. Full first-install evidence remains a separate release gate and must not be promoted from missing artifacts. | `contracts/app-release-channel.json`, release evidence manifests, remote release verification, and first-run VM evidence. |
| Full runtime executables must not require separate Gatekeeper approval | A Full install must not ask users to approve the App and then approve packaged runtime executables again. Distributable Full builds must sign native executables carried inside the runtime payload, verify the App bundle through `spctl`, verify packaged runtime native trust through `full-runtime-native-trust.json`, and upload that evidence with the Full release assets. Temporal CLI is packaged as an offline archive wrapper; clean VM smoke covers archive extraction and execution without a second user approval. | `scripts/build-full-first-install-package.ts`, `scripts/verify-full-runtime-native-trust.ts`, `.github/workflows/full-first-install-release.yml`, `contracts/app-release-channel.json`, and `scripts/verify-remote-release-assets.ts`. |
| Full size review cannot weaken offline first-install completeness | The Full package keeps the clean-machine offline first-install promise even when the compressed DMG exceeds the review threshold. The `750000000`-byte Full DMG threshold opens size review and optimization planning; it is not release-blocking by size alone and does not authorize removing required runtime, shell, native trust, or Core readiness payloads. Full optimization writes `package_optimization`, `full-app-bundle-trim-report.json`, and `full-package-boundary-audit.json` to prove explicit non-runtime pruning and boundary preservation; these artifacts do not replace clean VM smoke or owner receipt evidence. v26.6.21 measured `1121919153` Full DMG bytes and a zlib level 9 estimate of `844079932` bytes, so optimization starts with staged App/runtime non-runtime pruning and audit-only dedupe review before compression tuning. Standard and Full profiles stay separate: standard remains updater-visible and must not contain `opl-full-runtime`; Full remains first-install-only with bundled runtime. | `contracts/app-release-channel.json#full_first_install.size_budget`, `contracts/app-release-channel.json#full_first_install.size_policy.optimization_artifacts`, `docs/delivery/release/README.md#full-size-policy`, `scripts/analyze-full-package-size.ts`, `scripts/verify-remote-release-assets.ts`, and release-boundary tests. |
| Full runtime prune policy has one machine source | Full runtime size optimization follows the same maintainability pattern as mature packaging systems: exclusion rules are declarative, package contents have a dry-run/readback path, and app resources remain explicit package-boundary surfaces. The App repo owns `contracts/full-runtime-prune-policy.json` as the single machine-readable prune policy. Build filters, cache keys, Full manifest `runtime_prune_policy`, runtime assertions, and `npm run release:full:prune-audit -- --markdown` derive from that contract; code must not reintroduce divergent hard-coded prune lists. | `contracts/full-runtime-prune-policy.json`, `scripts/full-first-install-package.ts`, `scripts/audit-full-runtime-prune-policy.ts`, `scripts/README.md`, and release-boundary tests. |
| App docs use a lighter lifecycle taxonomy | The App keeps only App-owned long-lived docs directories. Framework/domain-agent taxonomy directories are added only when App material has a clear owner, purpose, state, and machine boundary. | `docs/docs_portfolio_consolidation.md`, `docs/README.md`, and repo docs inventory. |

## Superseded Readings

Rows in this table are retired interpretations kept as compact decision
provenance. They do not define current App behavior; current truth stays in the
`Current reading` column and the listed machine-boundary surfaces.

| Superseded reading | Current reading |
| --- | --- |
| AionUI implementation defaults can define One Person Lab App product truth. | AionUI is implementation material; App product truth is contract-backed from this repo. |
| Upstream AionUI multi-Agent controls are power-user App features. | They are implementation material or diagnostics; the App ordinary path is fixed Codex CLI plus configurable professional-agent shortcuts over installed packages. |
| Upstream AionUI overview/runtime/system/model/agent/tools/webui Settings tabs 是普通 App settings。 | 它们是 legacy 或 diagnostics routes，redirect 到 App-owned Overview、Maintenance & Updates、Data & Storage、Capabilities、Setup & Access、Preferences、Advanced、About 或 Update pages。 |
| App release or UI rendering can prove MAS/MAG/RCA/BookForge/OMA readiness. | App release artifacts, UI rendering, and provider/read-model availability are App or Framework evidence only; domain readiness remains domain-owned. |
| Full first-install assets can be served through the standard updater channel. | Full assets are separate first-install downloads or the explicit `one-person-lab-full` Homebrew cask and remain outside standard updater metadata. |
| A Homebrew receipt can prove the App is activated or release-ready. | Homebrew proves only that the tap resolved and installed or upgraded indexed files. User-state activation and readiness remain `opl system initialize --json` plus App release gates for the matching cohort. |
| A Nightly Homebrew tap can share the stable/latest formula target. | Nightly distribution remains an explicit prerelease/opt-in lane and points only at Nightly standard assets. It does not mark GitHub latest, select Full assets, or replace stable tap promotion. |
| AionUI builtin skills should be separately classified as App defaults. | AionUI builtin skills are candidate capabilities only. A skill enters the App package only through the App-level packaged whitelist or the explicit-only packaged list. |
| A polished external UI can be copied wholesale into OPL App or define the first screen. | External UIs such as PilotDeck may inform OPL-owned information organization, but source/runtime adoption must pass license, authority, adapter, package, and release-isolation gates first. The ordinary App surface remains chat-first with contextual inspector surfaces. |
| A WebUI candidate can define separate App state or runtime truth because it runs in a browser. | WebUI is only another delivery surface for the same chat-first App UI and must use the same renderer, bridge shape, product profile, Codex backend, and OPL state/action authority boundary. |
| A GUI feature inventory alone is enough to guide shell design. | Shell design must use the App-owned interaction spec and Codex-to-OPL delta first, then the feature inventory and machine contracts for implementation and validation. |
| Hermes Desktop was treated as the foreground alternative. | Hermes Desktop / `hermes-codex` is the prior foreground alternative reference. The current foreground alternative candidate is `opl-native-workbench` once the candidate registry is updated; AionUI remains the active release shell until `contracts/app-shell-adapter.json` changes and release gates pass. |
| AGUI proof should stay archived unless explicitly replayed. | AGUI is archived technical proof and explicit replay material only. It is not the foreground alternative and should not receive routine updates or polish unless AGUI is explicitly requested. |
