# One Person Lab App Decisions

Owner: `one-person-lab-app`
Purpose: `app_decisions`
State: `active_truth_with_history_notes`
Machine boundary: Human-readable decision record. Machine-readable truth lives in `contracts/`, source, release artifacts, updater metadata, test outputs, active shell validation, and OPL Framework CLI/read-model output consumed by the App.

## Current Decisions

| Decision | Current reading | Machine boundary |
| --- | --- | --- |
| App product truth is purpose-first | The App is a Codex wrapper for ordinary users to enter research, grant, presentation, and book work. It owns the App/product boundary and user path, while runtime, domain, memory, artifact, owner receipt, and production-readiness truth stay with OPL Framework and domain owners. | `contracts/app-product-profile.json#boundary`, `contracts/app-gui-product-contract.json#product_authority`, `docs/project.md`, and `docs/architecture.md`. |
| App repo owns GUI product truth | App-level page behavior, model-selection policy, onboarding policy, screenshots, release/user docs, and release gates are governed from this repo. | `contracts/app-gui-product-contract.json`, `contracts/app-product-profile.json`, `contracts/app-page-state-matrix.json`, `contracts/app-first-run-test-matrix.json`, release scripts, and App tests. |
| GUI mainline and alternative are fixed | The App GUI mainline is the OPL-branded AionUI shell. Hermes Desktop is the only foreground alternative candidate. AGUI/CopilotKit work is archived technical verification and is updated only when AGUI is explicitly requested. | `contracts/app-shell-adapter.json`, `contracts/app-shell-candidates.json#active_gui_mainline`, `contracts/app-shell-candidates.json#alternative_gui_policy`, and `contracts/shell-adapters/hermes-codex.json`. |
| GUI 定义栈由 App 拥有 | 理想交互细则、Codex-to-OPL 产品增量、GUI 能力清单和机器 contracts 构成一组定义栈。修改 shell 行为前，GUI implementation work 应按这个顺序阅读。 | `docs/product/gui/ideal-interaction-spec.md`, `docs/product/gui/codex-to-opl-app-delta.md`, `docs/product/gui/feature-inventory.md`, and `contracts/`. |
| Ordinary App executor is fixed to Codex CLI | The App home and ordinary conversation paths are a Codex App equivalent wrapper with built-in MAS/MAG/RCA/BookForge entries. Aion CLI, Claude Code, generic backend/provider switching, and permission-mode controls are not normal user choices; model control stays in the App-owned Codex model selector/status. | `contracts/app-gui-product-contract.json#executor_policy`, `contracts/app-product-profile.json#gui.home`, `contracts/app-page-state-matrix.json#guid_home`, active-shell validation, and shell GUI tests. |
| Model selector is visible and defaults strongest | Home and ordinary Codex conversations show the App default model as `GPT-5.5` through an App-owned model selector. Auto follows the latest strongest frontier model. Users can manually override both model and reasoning effort from the same model configuration menu; the ordinary input/status label does not repeat reasoning effort. Ordinary visible models are limited to GPT-5.5 and GPT-5.4, while models before GPT-5.4 stay retired/hidden for ordinary UI. Backend, executor, and permission selectors stay out of the ordinary path. | `contracts/app-gui-product-contract.json#executor_policy`, `contracts/app-page-state-matrix.json#guid_home`, `shells/aionui/.../oplProductProfile`, `GuidPage`, `AcpModelSelector`, `AcpSendBox`, and focused shell tests. |
| Settings/model/reasoning policy is single-sourced by App contracts | Settings Access / Model & Account, Home, ordinary Codex conversations, Aion, Hermes, and WebUI must derive default model, visible model list, reasoning effort options, and access-readiness presentation from the App product profile plus GUI executor policy. Shells render/adapt this policy only and must not fork a provider/model/reasoning policy or promote release/live evidence into Settings completion. | `contracts/app-settings-control-plane.json#model_reasoning_policy_source`, `contracts/app-product-profile.json#codex`, `contracts/app-product-profile.json#gui.home.codex_model_display_options`, `contracts/app-gui-product-contract.json#executor_policy`, active-shell validation, and focused release-boundary tests. |
| Conversation pending feedback shows elapsed seconds | Sending a message must immediately expose visible processing feedback with elapsed seconds while ACP/Codex is pending or running, even if thinking/tool events have begun. Raw trace remains diagnostic-only. | `contracts/app-gui-product-contract.json#pages.guid_home.conversation_feedback_policy`, `contracts/app-page-state-matrix.json#guid_home`, `ThoughtDisplay`, `AcpSendBox`, and focused shell tests. |
| First GUID message warms ACP before send | Fresh installs can spend their first ACP startup window resolving the Codex ACP adapter and local runtime state. GUID-created initial messages must await `conversation.warmup` before posting `/messages`, so cold startup happens on the explicit warmup path instead of racing the first user message into the 30s ACP handshake timeout. | Active shell `useAcpInitialMessage`, `warmupConversation`, shell focused tests, and `scripts/validate-active-shell.ts`. |
| Home assistants carry scoped skill profiles | MAS/MAG/RCA/BookForge are purpose entries over the fixed Codex executor. Each entry requires its matching Codex skill by default and exposes only relevant optional companion skills from the App packaged skill set. | `contracts/app-gui-product-contract.json#assistant_skill_profiles`, `contracts/app-product-profile.json#gui.assistant_skill_profiles`, `contracts/app-page-state-matrix.json#guid_home`, active-shell validation, and shell GUI tests. |
| Ordinary skill/MCP selection is OPL allowlist only | The ordinary Home composer and ordinary conversation loaded-capability surfaces use `gui.ordinary_capability_selector_policy`: visible skills are derived from MAS/MAG/RCA/BookForge assistant profiles, and visible MCP servers default to an empty App allowlist. AionUI implementation skills such as `aionui-skills`, `aionui-webui-setup`, `skill-creator`, `cron`, unlisted MCP servers, and disabled Team MCP snapshots such as `aionui-team`, `team_*`, and `mcp__aionui-team*` are excluded from ordinary selection, loaded-capability display, new conversation create payloads, and inherited session MCP state. | `contracts/app-product-profile.json#gui.ordinary_capability_selector_policy`, `contracts/app-gui-product-contract.json#ordinary_capability_selector_policy`, `contracts/app-page-state-matrix.json#guid_home`, active-shell validation, `GuidPage`, `useGuidSend`, `FileAttachButton`, `ConversationSkillsIndicator`, and shell whitelist tests. |
| Built-in assistant selection must be observable as a route receipt | Selecting MAS/MAG/RCA/BookForge creates a Codex CLI conversation and persists `route_kind`, `executor`, `assistant_id`, `assistant_short_name`, and `source`; the receipt proves the route without exposing a backend selector. | `contracts/app-gui-product-contract.json#builtin_assistant_route_receipt_policy`, `contracts/app-product-profile.json#gui.builtin_assistant_route_receipt_policy`, `contracts/app-page-state-matrix.json#guid_home`, active-shell validation, and shell unit tests. |
| Ordinary Settings navigation 由 App 拥有 | Settings is the App OPL Control Center. The target IA groups pages as Overview, Setup & Access, Capabilities, Maintenance & Updates, Data & Storage, Preferences, and Advanced; About is version/link/feedback information, while Updates & Maintenance owns normal update and repair actions. User-needed capabilities must be exposed as task entries such as Model & Account, Workspace, Maintenance Hub, Capability Status, Web / Remote Access, Developer Profile Status, External Tools & Voice, and Custom Assistant rather than as AionUI technical tabs. Legacy upstream settings routes remain compatibility redirects and must not become ordinary navigation. `contracts/app-settings-control-plane.json` hydrates the registry, ordinary and secondary route behavior, legacy redirects, extension anchor remaps, state/action source policy, `SettingsHost` / `SettingsShellAdapterSlot`, required Access/Environment/Storage/Capabilities view-model adapter entries, visual QA evidence policy, and `product_system_checklist` completion-audit source; `settings_ia.v1` remains the App GUI source contract. About/Update/Theme remain secondary or deep-link routes unless contract, matrix, validators, tests, and visual QA targets are changed together. Visual QA must cover the seven ordinary routes, bind desktop/mobile screenshots to a manifest, and keep Workspace/Local Services secondary-route evidence explicit; it is shell behavior evidence and does not prove release readiness, packaged App readiness, runtime currentness, or owner acceptance. `product_system_checklist` keeps installed/release currentness as an explicit release-owner-gated audit item so Settings completion cannot silently omit it or infer it from UI tests. | `docs/product/gui/settings-control-center.md`, `contracts/app-settings-control-plane.json`, `contracts/app-shell-adapter.json#implementation_probes.settings_control_plane_shell_adapter_slot`, `contracts/app-gui-product-contract.json#settings_navigation.settings_ia`, `contracts/app-page-state-matrix.json#pages`, `contracts/app-product-profile.json#settings`, active-shell validation, release-boundary tests, shell Settings/Guid tests, and active-shell Settings E2E screenshot manifest evidence. |
| AionUI Settings fork maintenance uses the existing control plane | Claude's broad fork-maintenance proposal is narrowed to the current repo architecture: enhance the App-owned Settings Control Plane, keep `SettingsHost` / `SettingsShellAdapterSlot` as a thin shell adapter, and require upstream intake to be `accepted`, `adapt`, `redirect`, or `reject` before any registry or slot change. The App does not create a new integration package, Settings plugin ecosystem, overlay topology, or shell-owned product IA to solve fork maintenance; numeric ROI claims are excluded unless backed by fresh evidence. Shell delta remains renderer, bridge, route/slot, styling/i18n, and focused test evidence only. | `docs/architecture/opl-aionui-fork-maintenance-strategy.md`, `docs/product/gui/settings-control-center.md`, `contracts/app-settings-control-plane.json`, `contracts/app-gui-product-contract.json#product_authority`, and `contracts/app-shell-adapter.json#implementation_probes.settings_control_plane_shell_adapter_slot`. |
| Home minimal command surface and Settings IA require matrix parity | Home is composer-first and does not show runtime activity, continue-work, per-assistant running badges, or footer quick icons. Runtime/secondary context refs plus General/Access/Capabilities/Local Environment/Advanced sections must be declared in both the GUI product contract and page-state matrix. | `contracts/app-gui-product-contract.json#pages`, `contracts/app-page-state-matrix.json#pages`, `scripts/validate-active-shell.ts`, and focused release-boundary GUI tests. |
| Skill is the public ABI; plugin is the App distribution shell | MAS/MAG/RCA remain domain skills backed by one action/stage metadata source. Codex App can expose them through plugin-packaged skills, while CLI/direct Codex paths use the same skill semantics. App install policy must not turn plugin-packaged domain skills into duplicate companion skill mirrors. | `contracts/app-install-exposure-policy.json`, `contracts/app-product-profile.json#companion_payloads`, OPL Framework `opl connect sync-skills`, and active-shell validation. |
| Codex configuration syncs App-visible OPL family plugins | Fresh Full installs must not require a second manual Codex plugin setup after the API key is configured. `opl system configure-codex --api-key-stdin` consumes the Full runtime module paths, syncs MAS/MAG/RCA plugin-packaged skills, generates the OMA and BookForge local plugin surfaces, and writes the Codex plugin registry in the same setup step. | OPL Framework `system configure-codex`, `syncFamilySkillPacks`, App Full runtime wrappers, and release-boundary wrapper validation. |
| Codex fallback uses an offline archive wrapper | The Full DMG remains self-contained on a clean machine without carrying the expanded Codex vendor binary directly in `runtime/current/bin`. The packaged fallback is `runtime/current/bin/codex` as a shell wrapper plus `runtime/current/vendor/codex/codex_cli_darwin_arm64.tar.gz`; `--version` stays fast and real Codex execution extracts the archive into the installed runtime cache before exec. | `contracts/app-release-channel.json#full_first_install.required_payloads.codex_cli`, `scripts/build-full-first-install-package/runtime-layers.ts`, `scripts/verify-remote-release-assets.ts`, and Full clean-VM smoke evidence. |
| First conversation waits for ACP warmup, not Full readiness | First-run Core readiness still requires only workspace root, Codex CLI, and Codex config before `/guid`. The first `/guid` message must wait for the conversation record and ACP warmup before `sendMessage`, so slow first-run dependency unpacking does not become a fixed 30-second ACP handshake failure. Domain modules, Codex plugin sync, and Temporal provider readiness stay visible background maintenance. | `contracts/app-install-exposure-policy.json#setup_flow_contract.first_conversation_readiness`, `contracts/app-product-profile.json#first_run.first_conversation`, active-shell `useAcpInitialMessage`, and shell unit tests. |
| First-run preparation is visible but skippable after Codex core is available | A first-run checklist may carry real OPL preparation tasks because it gives users visible progress, but non-core/network-dependent preparation must not trap users before the App. Once Codex CLI / Codex adapter can start, the user can skip into the chat-first shell; the skip writes a `user_deferred` routing marker and must not claim gflabtoken API key configured, module reconcile complete, MAS/MAG/RCA domain ready, or Full OPL readiness. AionUI may adopt this pattern only through the active-shell first-run contract and release-shell validation. | `docs/product/shell-alternatives/hermes-first-run-flow.md`, `contracts/shell-adapters/hermes-codex.json#first_run_contract.skip_to_chat_policy`, `contracts/app-shell-candidates.json#hermes-codex.first_run_contract`, and Hermes candidate focused tests. |
| Temporal defaults are App-configured and OPL-owned | The App may configure default Temporal environment for the packaged OPL runtime, but it does not own provider implementation or runtime truth. Full runtime wrappers export `OPL_FAMILY_RUNTIME_PROVIDER=temporal`, `OPL_TEMPORAL_ADDRESS=127.0.0.1:7233`, `OPL_TEMPORAL_NAMESPACE=default`, and `OPL_TEMPORAL_TASK_QUEUE=opl-stage-attempts`; the bundled Temporal CLI fallback is an offline archive wrapper over `runtime/current/vendor/temporal/temporal_cli_darwin_arm64.tar.gz`, extracting into the installed runtime cache only when Temporal is actually executed. OPL Framework owns service start, worker status/start, residency proof, lifecycle diagnostics, and repair receipts. | `contracts/app-install-exposure-policy.json#temporal_auto_configuration`, `contracts/app-release-channel.json#full_first_install.required_payloads.temporal_runtime_provider`, `scripts/full-first-install-runtime-wrappers.ts`, `scripts/verify-full-runtime-native-trust.ts`, and OPL Framework family-runtime commands. |
| Install/update taxonomy has seven user-facing layers | App maintenance is explained as Installation Carrier, OPL Runtime Fabric, Capability Packages, Companion Tools, Codex Surface, Workflow Profile, and User Data/Artifacts. `runtime_substrate` remains the machine id, but user-facing docs and Settings use OPL Runtime Fabric. Full first-install assets preload payloads for clean machines; they are not a long-term update channel. The standard updater is macOS App carrier only. Docker/WebUI image updates require carrier status, host update route, `host_executor_required` or `manual_required`, and mounted data/projects preservation proof. `embedded_codex_executor` means the App-owned Codex CLI executor payload and must not mutate a user's global Codex/Homebrew/system install. OPL Packages may silently update only clean managed roots; dirty/developer/manual-required states fail closed. OPL Flow Workflow Profile material must not silently overwrite existing `AGENTS.md` or `TASTE.md`; existing profiles route through Codex semantic merge packets. | `contracts/app-release-channel.json#standard_updater`, `contracts/app-release-channel.json#managed_update_plane`, `contracts/app-install-exposure-policy.json#runtime_substrate_auto_update`, `contracts/app-install-exposure-policy.json#agent_installation_contract`, `docs/architecture.md`, and `docs/delivery/release/README.md#install-and-update-taxonomy`. |
| OPL Runtime Fabric is the shared runtime foundation, not a capability module | Ordinary App users get quiet maintenance for App-owned runtime roots grouped as Agent Execution Core (`embedded_codex_executor`, Temporal archive, OPL Framework runtime), Environment Base (Node/Python/uv), and OPL System Bridge (native helper). MAS/MAG/RCA/OMA/BookForge/ScholarSkills package payloads are Capability Packages, not OPL Runtime Fabric. Companion tools such as OfficeCLI and MinerU may share the managed updater kernel but remain a separate class. OPL Runtime Fabric maintenance must not write Electron `latest*.yml`, make Full assets updater-visible, or silently mutate user global Homebrew/system Codex or Temporal installs. System PATH, Homebrew, global tools, and developer checkouts are diagnostic or explicit expert opt-in unmanaged sources, not ordinary defaults. | `contracts/app-release-channel.json#runtime_substrate_updater`, `contracts/app-release-channel.json#managed_update_plane.planes[runtime_substrate]`, `scripts/validate-active-shell/release-contract-validator.ts`, `tests/release/app-release-boundary-cases/workflow-release-channels.ts`, `docs/delivery/release/README.md#homebrew-distribution-boundary`, and OPL Framework `opl update apply/repair/rollback --json` runner outputs. |
| OPL Framework artifact evidence is an App release gate | The OPL Framework runtime artifact is part of OPL Runtime Fabric release readiness. A candidate must prove the Framework artifact channel, artifact readback, sha256 checksum, and rollback ref/receipt before App release gates can pass. The App consumes refs/checksums/status only and must not read artifact bodies, implement the Framework update kernel, or treat this as permission to update a Docker/WebUI image from inside Docker. Docker/WebUI image replacement stays an Installation Carrier host route. | `contracts/app-release-channel.json#runtime_substrate_updater.framework_artifact_gate`, `contracts/app-release-channel.json#managed_update_plane.release_boundary_required_cases`, `scripts/validate-active-shell/managed-update-plane-validator.ts`, `tests/release/app-release-boundary-cases/workflow-release-channels/managed-update-plane.ts`, and OPL Framework artifact receipts consumed through `opl update status/check/plan/apply/repair/rollback --json`. |
| GHCR OPL Packages are the ordinary non-development Capability Packages source | MAS/MAG/RCA/OMA/BookForge/ScholarSkills ordinary users consume managed GHCR OPL Packages, not Git checkouts. Nightly package publication is the automation cadence for this package source, not a developer source. Managed package roots update silently only when the Framework reports clean managed eligibility and must report component receipts plus post-apply sync status for Codex plugin registry, plugin-packaged skills, generated OMA/BookForge surfaces, ScholarSkills package surface, Codex Surface readiness, and App/Codex plugin-cache reload guidance. | `contracts/app-install-exposure-policy.json#agent_installation_contract.managed_agent_pack_distribution`, `contracts/app-release-channel.json#homebrew_tap_distribution.agent_pack_policy`, `contracts/app-release-channel.json#managed_update_plane.planes[capability_packages]`, OPL Framework `module-package-channel.ts`, `startup-maintenance.ts`, and package-channel/startup-maintenance tests. |
| Local Environment owns the ordinary OPL Packages maintenance entry | The App does not add a new top-level Settings tab for package maintenance. Local Environment carries the user-friendly MAS/MAG/RCA/OMA/BookForge/ScholarSkills OPL Packages status, Codex Surface readiness substatus, recommended action, and manual check/apply/repair/rollback mappings for capability packages only. Updates & Maintenance keeps the advanced unified detail view: managed runtime/capability/companion components use `opl update status/check/apply/repair/rollback` or App state/action routes, while installation carrier updates use carrier-specific host update route refs; it must not implement the update kernel, silently mutate developer or dirty checkouts, or write runtime/domain truth. | `contracts/app-gui-product-contract.json#pages.settings_environment.module_maintenance_entry`, `contracts/app-page-state-matrix.json#environment`, `scripts/validate-active-shell/gui-product-contract-validator.ts`, and `scripts/validate-active-shell/page-state-app-settings-validator.ts`. |
| Local data cleanup is inventory-first and artifact-safe | Updater installer packages, conversation artifacts, OPL Runtime Fabric roots, and logs are separate lifecycle classes. The App may clean stale updater installer cache packages, but conversation workdirs and generated deliverables require inventory, archive/export, restore proof, and explicit user confirmation before deletion. OPL Runtime Fabric prune follows current/rollback pointers and starts as dry-run; logs use bounded rotation and cannot stand in for artifact cleanup evidence. | `contracts/app-release-channel.json#local_data_lifecycle`, active-shell validation, `shells/aionui/packages/desktop/src/process/services/localDataLifecycle/index.ts`, focused shell lifecycle tests, and historical Issue #5 provenance in `docs/history/process/local-data-lifecycle-issue-5.md`. |
| OPL Packages channel failures fail closed with packaged fallbacks | A missing or unavailable GitHub Packages/GHCR OPL package tag is a package-channel failure, not a reason to hand-install a divergent OMA or ScholarSkills surface. The source order is bundled Full runtime modules, App/CLI-managed GHCR OPL Packages channel, then explicit Developer Profile checkout override. Failures surface as actionable background-maintenance errors while preserving one Codex-visible plugin/skill semantic path. | `contracts/app-install-exposure-policy.json#agent_installation_contract.managed_agent_pack_distribution`, `npm run validate:agent-installation`, Full runtime module path wrappers, and OPL Framework `opl connect reconcile-modules` / `opl connect sync-skills`. |
| App packaged skills use one whitelist | `default_packaged_codex_skill_ids` is the only App product default for skills that are both packaged and default visible. Candidate source is intentionally ignored: AionUI builtin, Skills Manager, Codex local skill, and plugin-packaged skill are all sources to copy from, not separate product policies. `packaged_not_default_visible_codex_skill_ids` is reserved for explicit-only packaged skills such as OMA. | `contracts/app-product-profile.json#companion_payloads`, Full first-install package scripts, full package manifest, and active-shell validation. |
| Settings builtin skills are filtered through the App whitelist | The active shell may report upstream builtin skills and builtin-auto skills, but ordinary Agents & Capabilities only displays builtin catalog skills from the App packaged skill set and auto-injected skills from `default_packaged_codex_skill_ids`. AionUI helper skills such as `aionui-skills` do not surface as OPL App capabilities. | `contracts/app-gui-product-contract.json#pages.settings_capabilities`, `contracts/app-page-state-matrix.json#capabilities`, `contracts/app-product-profile.json#companion_payloads.default_packaged_codex_skill_ids`, `SkillsHubSettings`, active-shell validation, and shell tests. |
| Active shell stays an external implementation carrier | `shells/aionui/` is the current active shell checkout from `gaofeng21cn/opl-aion-shell`; shell history and implementation truth stay in the shell repo. | `contracts/app-shell-adapter.json` and active-shell validation decide whether a shell satisfies the App contract. |
| Active shell upstream intake is classified before release | Upstream AionUI features must be classified before an App release can consume them. Settings-specific intake uses fixed `accepted`, `adapt`, `redirect`, or `reject` buckets before any registry or adapter slot change; broader shell intake keeps its release-gate classifications for non-Settings surfaces. AionUI Team is rejected for ordinary surfaces; MCP snapshot scrub and mutation/route gates are required implementation probes, not prose-only guidance. | `contracts/app-settings-control-plane.json#upstream_intake_checklist`, `contracts/app-settings-control-plane.json#upstream_intake_classification`, `contracts/app-shell-adapter.json#upstream_intake`, `contracts/app-shell-adapter.json#implementation_probes`, `contracts/app-gui-product-contract.json#ordinary_capability_selector_policy.forbidden_mcp_matchers`, `contracts/app-product-profile.json#gui.ordinary_capability_selector_policy.forbidden_mcp_matchers`, `scripts/validate-active-shell.ts`, and release-boundary tests. |
| Runtime page is user task status first | The App consumes `opl app state --profile fast --json` for the default user task view and `opl runtime app-operator-drilldown --json` only as secondary runtime diagnostics. Its daily Runtime page first answers: how many tasks are running, how many projects/tasks are active, how many are queued, how many need attention, and where each task is now. Required user fields are `running_task_count`, `active_project_count`, `queued_project_count`, `attention_count`, plus per-task title, status, stage, progress label, next step, owner, and last progress. Temporal/provider/projection/ref/stage attempt/ledger/current_control_state terminology must not be default-visible. `running_provider_attempt_count` may include checkpointed provider refs and remains diagnostic; it must not be displayed as the user running task count. Full detail stays on demand, actions stay safe dry-run capable, and all task/project lines remain refs-only display projections. The App does not own runtime truth, provider implementation, domain truth, progress truth, memory body, artifact body, owner receipt authority, action route authority, or domain verdicts. | `contracts/app-runtime-bridge.json`, `contracts/app-page-state-matrix.json`, `contracts/app-gui-product-contract.json#pages.runtime_status`, `contracts/fixtures/opl-app-state-fast.fixture.json`, and OPL Framework CLI/read-model output. |
| Provider readiness repair stays secondary and actionable | When App state exposes provider readiness blockers, the Runtime page must give an explicit OPL repair command: `worker_not_ready` points to `opl family-runtime worker start --provider temporal`, while missing Temporal Search Attributes point to `opl family-runtime provider repair --provider temporal`. These are provider liveness/visibility repair paths only. They must not claim domain readiness, App release readiness, family production readiness, owner receipt authority, typed blocker authority, or override `current_owner_delta` as the default owner-action payload. | `contracts/app-runtime-bridge.json#provider_readiness_repair_projection`, `contracts/app-page-state-matrix.json#runtime.runtime_view_model.provider_readiness_repair`, `contracts/app-gui-product-contract.json#framework_surfaces.provider_readiness_repair`, `scripts/validate-active-shell.ts`, and `tests/release/app-release-boundary-cases/runtime-page-evidence-boundary.ts`. |
| Runtime State Index sidecar is read-model only | The App consumes OPL State Index Kernel / SQLite sidecar output only as Framework-projected read-model refs. The allowed path is App state JSON, explicit full App state, or task operator drilldown JSON; refs can drill down to Stage Folder locations. The App cannot directly read or write SQLite sidecar files, mutate the State Index Kernel, write domain truth, create owner receipts, read artifact bodies, or authorize readiness, artifact authority, quality/export verdicts, App release readiness, or family production readiness. | `contracts/app-runtime-bridge.json#state_index_sidecar_projection`, `contracts/app-page-state-matrix.json#runtime.runtime_view_model.state_index_sidecar`, `contracts/app-gui-product-contract.json#pages.runtime_status.state_index_sidecar_policy`, `contracts/fixtures/opl-app-state-fast.fixture.json`, and OPL Framework State Index Kernel projection output. |
| Runtime task drilldown consumes artifact-native refs only | The App consumes OPL Stage Artifact Kernel workbench refs for current pointer, canonical artifacts, export artifacts, lineage, retention, conformance, manifest/hash/receipt/blocker refs. This is a display/read-model contract only: the App does not read artifact bodies, own artifact authority, or declare MAS/MAG/RCA/OMA quality verdict, export readiness, domain readiness, App release readiness, or family production readiness. | `contracts/app-runtime-bridge.json#artifact_native_drilldown_projection`, `contracts/app-page-state-matrix.json#runtime.runtime_view_model.artifact_native_drilldown`, `contracts/app-gui-product-contract.json#pages.runtime_status.artifact_native_drilldown_policy`, `contracts/fixtures/opl-app-state-fast.fixture.json`, and OPL Framework `contracts/opl-framework/stage-artifact-runtime-contract.json`. |
| AionUI Team is hidden in ordinary OPL App | Upstream AionUI Team is configured around shell-local team leaders and agents, so it is not an OPL ordinary-user capability. The active shell keeps Team mode disabled, hides the Team sidebar entry, no-ops Team-created redirects, rejects Team deep links, redirects compatible `/team/*` routes back to the App-owned home path, and scrubs disabled Team MCP state from ordinary conversations before rendering or inheriting it. | `contracts/app-gui-product-contract.json#settings_navigation.team_surface_policy`, `contracts/app-gui-product-contract.json#ordinary_capability_selector_policy`, `contracts/app-page-state-matrix.json`, `scripts/validate-active-shell.ts`, and active shell `TEAM_MODE_ENABLED` plus ordinary conversation Team MCP scrub tests. |
| Release/user-path evidence is cohort-bound | App release and user-path evidence can support a same-cohort App release review, but draft bundles, verified refs, updater assets, screenshots, and packaged route smoke do not prove stable/latest promotion, MAS/MAG/RCA/BookForge readiness, or OPL family production readiness. | `contracts/app-release-channel.json#operator_evidence_bundle`, release evidence manifests, remote release verification, and release owner promotion records. |
| Developer Profile expresses capabilities, not one big switch | Ordinary users get Capability Package updates through Framework/App maintenance after the App is installed. Developer behavior is shown as a Developer Profile with explicit capability axes: `source_channel`, `workspace_trust`, `github_authority`, `agent_automation`, and `runtime_mutation_scope`. GitHub repo or local checkout module sources are an explicit `source_channel` opt-in, surfaced in Advanced/Local Environment from `app_state.developer_profile` and `app_state.modules[].source_policy`; raw `OPL_MODULE_SOURCE_MODE` remains a low-level diagnostic/CI override, not normal Settings UI. `opl-flow` remains a Workflow Profile plugin, outside updater assets, WebUI GHCR image publishing, and `one-person-lab-modules/*`. | `contracts/app-gui-product-contract.json#developer_profile`, `contracts/app-gui-product-contract.json#module_path_source_policy`, `contracts/app-page-state-matrix.json#environment`, `contracts/app-product-profile.json#settings.developer_profile`, `contracts/app-first-run-test-matrix.json#updater_standard_channel`, and active-shell validation. |
| Homebrew is App cask transport and index only | Homebrew points terminal users and automation at the same App release cohorts through `one-person-lab`, `one-person-lab-nightly`, and explicit stable `one-person-lab-full` casks; it does not own App activation, user workspace state, module readiness, agent-pack distribution, skill/plugin semantics, updater policy, or stable/latest promotion. After a Homebrew install or upgrade, the activation source is still `opl system initialize --json`, with `opl install`, `opl system startup-maintenance`, `opl connect reconcile-modules`, and `opl connect sync-skills` handling user-state, modules, agent packs, and Codex-visible exposure. New stable desktop releases update the stable `one-person-lab` cask by direct tap commit only after the promote workflow publishes the draft release, then run the Homebrew VM gate against that published cask; existing published release refreshes may update the tap after remote asset verification and before the Homebrew VM gate. Nightly tap updates stay opt-in/prerelease and point only at standard macOS arm64 assets; Full tap updates stay stable-only and point only at `One-Person-Lab-Full-*` plus `full-package-manifest.json`. Tap freshness is primarily owned by the tap repo's self-sync workflow; same-owner App release tap writes use direct commits only and must not open tap pull requests. | `docs/delivery/release/README.md#homebrew-distribution-boundary`, `contracts/app-release-channel.json`, `contracts/app-install-exposure-policy.json#setup_flow_contract`, `contracts/app-install-exposure-policy.json#agent_installation_contract`, Homebrew tap workflow/check output, and OPL Framework `opl system initialize --json`. |
| WebUI uses the same chat-first authority as desktop | WebUI is a delivery surface for the same chat-first App UI, not a second product profile, runtime source, provider selector, memory store, artifact store, release channel, or full workbench first screen. Current foreground candidate work should apply this through Hermes only if Hermes WebUI is explicitly claimed; AGUI WebUI evidence remains archived technical proof. | `contracts/app-shell-candidates.json#alternative_gui_policy`, `docs/product/gui/feature-inventory.md`, and candidate-specific smoke evidence when explicitly claimed. |
| Docker/WebUI beginner path is a non-live functional path before release evidence | The beginner WebUI path is one-click installer first, browser Access API key entry, runtime proxy smoke through `opl system configure-codex --api-key-stdin --json`, startup diagnostics, data/project preservation, host-side image update route, and ordinary user progress after WebUI opens. Local and optional remote image digest comparison may show `current` or `update_available`, but it is status guidance only and does not prove release-ready, live latest/currentness, or applied host update. This closes the App contract/guide/validator path only; clean VM, GHCR receipt, live provider access, owner acceptance, and release-ready remain release-owner evidence. | `contracts/app-install-exposure-policy.json#installer_surfaces.docker_webui.installer_model`, `scripts/install-docker-webui.sh`, `scripts/install-docker-webui.ps1`, `scripts/docker-webui-smoke-gate.ts`, `scripts/validate-docker-webui-diagnostics.ts`, `docs/guides/docker-webui-install/guide.qmd`, and Docker/WebUI focused release-boundary tests. |
| Hermes candidate follows a three-phase compatibility route | Hermes is the only foreground alternative candidate, not the active release shell. Phase 1 is a compatibility firewall: implement Codex App-like ordinary paths, adapt surfaces whose semantics match OPL/Codex, move diagnostic-only backend detail to Advanced/Diagnostics, and hide full-Hermes backend features that would mislead ordinary users. Phase 2 presents OPL branded Codex features such as MAS/MAG/RCA and model access as product surfaces. Phase 3 evaluates the full OPL product profile and any active-shell adoption. MAS/MAG/RCA invocation is Codex Skill/Plugin based through slash shortcuts and structured Codex skill input, not GUI purpose-route authority. | `docs/product/shell-alternatives/hermes-gui-adaptation-plan.md`, `docs/product/shell-alternatives/hermes-first-run-flow.md`, `docs/active/app-ideal-state-gap-plan.md#hermes_codex_candidate_gate`, `contracts/app-shell-candidates.json`, and `contracts/shell-adapters/hermes-codex.json`. |
| External UI projects can inform information organization without becoming authority | PilotDeck is a reference for optional workspace/session rail, chat-first main pane, grouped files, memory, routing, and always-on context. Its AGPL source and runtime are excluded unless a separate license and candidate-adoption decision is made, and it is not a first-screen workbench template. | `contracts/app-shell-candidates.json#design_references` and `docs/product/gui/feature-inventory.md#PilotDeck-Informed Information Organization`. |
| Standard updater and Full first-install assets stay separate | Standard updater metadata must never select `Full` assets. Full first-install evidence remains a separate release gate and must not be promoted from missing artifacts. | `contracts/app-release-channel.json`, release evidence manifests, remote release verification, and first-run VM evidence. |
| Full runtime executables must not require separate Gatekeeper approval | A Full install must not ask users to approve the App and then approve packaged runtime executables again. Distributable Full builds must sign native executables carried inside the runtime payload, verify the App bundle through `spctl`, verify packaged runtime native trust through `full-runtime-native-trust.json`, and upload that evidence with the Full release assets. Temporal CLI is packaged as an offline archive wrapper; clean VM smoke covers archive extraction and execution without a second user approval. | `scripts/build-full-first-install-package.ts`, `scripts/verify-full-runtime-native-trust.ts`, `.github/workflows/full-first-install-release.yml`, `contracts/app-release-channel.json`, and `scripts/verify-remote-release-assets.ts`. |
| Full size review cannot weaken offline first-install completeness | The Full package keeps the clean-machine offline first-install promise even when the compressed DMG exceeds the review threshold. The `750000000`-byte Full DMG threshold opens size review and optimization planning; it is not release-blocking by size alone and does not authorize removing required runtime, shell, native trust, or Core readiness payloads. Full optimization writes `package_optimization`, `full-app-bundle-trim-report.json`, and `full-package-boundary-audit.json` to prove explicit non-runtime pruning and boundary preservation; these artifacts do not replace clean VM smoke or owner receipt evidence. v26.6.21 measured `1121919153` Full DMG bytes and a zlib level 9 estimate of `844079932` bytes, so optimization starts with staged App/runtime non-runtime pruning and audit-only dedupe review before compression tuning. Standard and Full profiles stay separate: standard remains updater-visible and must not contain `opl-full-runtime`; Full remains first-install-only with bundled runtime. | `contracts/app-release-channel.json#full_first_install.size_budget`, `contracts/app-release-channel.json#full_first_install.size_policy.optimization_artifacts`, `docs/delivery/release/README.md#full-size-policy`, `scripts/analyze-full-package-size.ts`, `scripts/verify-remote-release-assets.ts`, and release-boundary tests. |
| Full runtime prune policy has one machine source | Full runtime size optimization follows the same maintainability pattern as mature packaging systems: exclusion rules are declarative, package contents have a dry-run/readback path, and app resources remain explicit package-boundary surfaces. The App repo owns `contracts/full-runtime-prune-policy.json` as the single machine-readable prune policy. Build filters, cache keys, Full manifest `runtime_prune_policy`, runtime assertions, and `npm run release:full:prune-audit -- --markdown` derive from that contract; code must not reintroduce divergent hard-coded prune lists. | `contracts/full-runtime-prune-policy.json`, `scripts/full-first-install-package.ts`, `scripts/audit-full-runtime-prune-policy.ts`, `scripts/README.md`, and release-boundary tests. |
| App docs use a lighter lifecycle taxonomy | The App keeps only App-owned long-lived docs directories. Framework/domain-agent taxonomy directories are added only when App material has a clear owner, purpose, state, and machine boundary. | `docs/docs_portfolio_consolidation.md`, `docs/README.md`, and repo docs inventory. |

## Superseded Readings

Rows in this table are retired interpretations kept as compact decision
provenance. They do not define current App behavior; current truth stays in the
`Current reading` column and the listed machine-boundary surfaces.

| Superseded reading | Current reading |
| --- | --- |
| AionUI implementation defaults can define One Person Lab App product truth. | AionUI is implementation material; App product truth is contract-backed from this repo. |
| Upstream AionUI multi-Agent controls are power-user App features. | They are implementation material or diagnostics; the App ordinary path is fixed Codex CLI plus built-in MAS/MAG/RCA/BookForge purpose entries. |
| Upstream AionUI overview/runtime/system/model/agent/tools/webui Settings tabs 是普通 App settings。 | 它们是 legacy 或 diagnostics routes，redirect 到 App-owned General、Local Environment、Storage、Agents & Capabilities、Access、Appearance、Advanced 或 About & Updates pages。 |
| App release or UI rendering can prove MAS/MAG/RCA/BookForge/OMA readiness. | App release artifacts, UI rendering, and provider/read-model availability are App or Framework evidence only; domain readiness remains domain-owned. |
| Full first-install assets can be served through the standard updater channel. | Full assets are separate first-install downloads or the explicit `one-person-lab-full` Homebrew cask and remain outside standard updater metadata. |
| A Homebrew receipt can prove the App is activated or release-ready. | Homebrew proves only that the tap resolved and installed or upgraded indexed files. User-state activation and readiness remain `opl system initialize --json` plus App release gates for the matching cohort. |
| A Nightly Homebrew tap can share the stable/latest formula target. | Nightly distribution remains an explicit prerelease/opt-in lane and points only at Nightly standard assets. It does not mark GitHub latest, select Full assets, or replace stable tap promotion. |
| AionUI builtin skills should be separately classified as App defaults. | AionUI builtin skills are candidate capabilities only. A skill enters the App package only through the App-level packaged whitelist or the explicit-only packaged list. |
| A polished external UI can be copied wholesale into OPL App or define the first screen. | External UIs such as PilotDeck may inform OPL-owned information organization, but source/runtime adoption must pass license, authority, adapter, package, and release-isolation gates first. The ordinary App surface remains chat-first with contextual inspector surfaces. |
| A WebUI candidate can define separate App state or runtime truth because it runs in a browser. | WebUI is only another delivery surface for the same chat-first App UI and must use the same renderer, bridge shape, product profile, Codex backend, and OPL state/action authority boundary. |
| A GUI feature inventory alone is enough to guide shell design. | Shell design must use the App-owned interaction spec and Codex-to-OPL delta first, then the feature inventory and machine contracts for implementation and validation. |
| AGUI proof should stay archived unless explicitly replayed. | AGUI is archived technical proof and explicit replay material only. It is not the foreground alternative and should not receive routine updates or polish unless AGUI is explicitly requested. |
