# One Person Lab App Decisions

Owner: `one-person-lab-app`
Purpose: `app_decisions`
State: `active_truth_with_history_notes`
Machine boundary: Human-readable decision record. Machine-readable truth lives in `contracts/`, source, release artifacts, updater metadata, test outputs, active shell validation, and OPL Framework CLI/read-model output consumed by the App.

## Current Decisions

| Decision | Current reading | Machine boundary |
| --- | --- | --- |
| App product truth is purpose-first | The App is a Codex wrapper for ordinary users to enter research, grant, presentation, and book work. It owns the App/product boundary and user path, while runtime, domain, memory, artifact, owner receipt, and production-readiness truth stay with OPL Framework and domain owners. | `contracts/app-product-profile.json#boundary`, `contracts/app-gui-product-contract.json#product_authority`, `docs/project.md`, and `docs/architecture.md`. |
| App repo owns GUI product truth | App-level page behavior, model-selection policy, onboarding policy, screenshots, release/user docs, and release gates are governed from this repo. | `contracts/app-gui-product-contract.json`, `contracts/app-product-profile.json`, `contracts/app-page-state-matrix.json`, `contracts/app-first-run-test-matrix.json`, release scripts, and App tests. |
| X0-01 Runtime is a conditionally retained minimal project status route | When explicitly enabled, Runtime shows only Agent -> Project -> Work Item status, whether work is running, elapsed time, current and total Token usage, and a clickable Stage surface with complete Stage order, next Stage, and current Attempt. The default row never exposes raw runtime internals. Task archive/restore is the only Runtime mutation. Provider/platform repair, managed dependencies, updates, raw diagnostics, State Index, and operator drilldown belong to Maintenance; Agent Package lifecycle belongs to Agents; Skill/Plugin/Flow and local capability health belongs to Capabilities; artifact provenance belongs to Inspector; complete same-cohort evidence belongs to release tooling. The route is not a core requirement, default release gate, or Native phase-1 parity target; default gates keep only its Framework producer/authority contract and route detail is validated explicitly through `validate:runtime-route`. | `contracts/app-gui-product-contract.json#pages.runtime_status.runtime_cockpit_product_contract`, `contracts/app-runtime-bridge.json#runtime_progress_page_display_policy`, `contracts/app-page-state-matrix.json#pages[id=runtime]`, `docs/product/gui/runtime-overview-redesign.md`, and the explicit Runtime route tests. |
| macOS tray uses a dedicated OPL template mark | The menu-bar icon is a 16 pt monochrome geometric reduction of the OPL segmented workflow orbit with a single-person core. It uses transparent 1x/2x template assets and must not be a scaled copy of the full-color application icon; other desktop platforms retain the application icon until they define a platform-specific tray asset. | `contracts/app-gui-product-contract.json#desktop_tray_policy.icon_policy`, active-shell tray implementation, packaged resources, and focused shell tests. |
| GUI mainline and alternatives are fixed | The App GUI mainline is the OPL-branded AionUI shell. `opl-native-workbench` is the foreground alternative selected by `contracts/app-shell-candidates.json` and governed by its own adapter. Hermes Desktop / `hermes-codex` is a retained reference and AGUI/CopilotKit is archived proof; both are role tombstones in the active registry, with detail owned by adapters and replay runbooks. Default validation checks only fixed role integrity. Native, Hermes, and AGUI detail validation is explicit on demand and never changes active-shell adoption. | `contracts/app-shell-adapter.json`, `contracts/app-shell-candidates.json#active_gui_mainline`, `contracts/app-shell-candidates.json#alternative_gui_policy`, `contracts/shell-adapters/opl-native-workbench.json`, `contracts/shell-adapters/hermes-codex.json`, `docs/product/gui/gui-shell-candidates.md`, and `package.json#scripts`. |
| GUI 三层定义栈由 App 拥有 | GUI 定义顺序固定为 `gui_definition_stack: product_definition > visual_system > shell_implementation_conformance`。第一层由 GUI README、能力清单和 App contracts 定义功能；第二层由理想交互、visual system、Codex-to-OPL 增量和 element audit 定义理想交互与视觉；第三层由 shell implementation guide 与 conformance matrix 约束实现和偏差。层级只能向下实现，`gui_shell_authority: implementation_only`，shell、截图、上游默认和局部实现都不能反向定义产品。 | `docs/product/gui/README.md`, `docs/product/gui/feature-inventory.md`, `docs/product/gui/ideal-interaction-spec.md`, `docs/product/gui/visual-system.md`, `docs/product/gui/codex-to-opl-app-delta.md`, `docs/product/gui/element-audit.md`, `docs/product/gui/shell-implementation-guide.md`, `docs/product/gui/shell-conformance-matrix.md`, and `contracts/app-shell-candidates.json#design_system_governance`. |
| 外部交互对齐不得删除 OPL 核心特色能力 | Codex 等外部产品只提供 composition、placement 和 interaction 参考，不拥有 OPL 功能集合。B0/R1/U1 能力可以调整位置，但旧入口只能在同一变更已经提供可见、键盘可达的替代入口，并同步更新 contract、source 和 tests 后移除。跨项目 Runtime 属 X0-01：可保留为条件 route，但不受核心 preservation gate、普通首页固定入口或默认 release gate 保护；会话级 current-task context 独立成立。 | `contracts/app-gui-product-contract.json#interaction_baseline.feature_preservation_policy`, `docs/product/gui/codex-to-opl-app-delta.md`, active-shell navigation source, GUI validator, and navigation DOM tests. |
| GUI reference and implementation deviation stay explicit | The current visual pixel baseline is ChatGPT Codex macOS `26.707.72221` / build `5307`, observed on `2026-07-15`; `26.707.41301` remains an interaction observation only. The App-owned ideal target keeps the desktop workspace/session rail visible and the inspector closed by default. Active AionUI state is read dynamically from the product profile and compared with that ideal; no decision record copies the current active value. | `docs/product/gui/visual-system.md`, `docs/product/gui/codex-app-visual-parity.md`, `contracts/app-shell-candidates.json#design_system_governance`, `contracts/app-shell-candidates.json#candidates[opl-native-workbench].target_product_shape`, and `contracts/app-product-profile.json#gui.home.home_layout`. |
| Ordinary App executor is fixed to Codex CLI | The App home and ordinary conversation paths are a Codex App equivalent wrapper with configurable professional-agent shortcuts over installed packages. Aion CLI, Claude Code, and generic backend/provider switching are not normal user choices. The ordinary composer keeps the App-owned model/reasoning control and a user-language permission/access control without exposing provider or backend terminology. | `contracts/app-gui-product-contract.json#executor_policy`, `contracts/app-product-profile.json#gui.home`, `contracts/app-page-state-matrix.json#guid_home`, `docs/product/gui/agent-package-management-plan.md`, active-shell validation, and shell GUI tests. |
| Session/thread is identity; Project affinity is zero-or-one | A Codex App Server thread is the conversation identity. A new session may start with one Project/directory affinity or projectless; projectless means `custom_workspace=false` or no canonical recorded cwd, not no OS/runtime `pwd`. A projectless session may be adopted once while preserving thread identity and history: call existing `thread/settings/update.cwd`, require exact `thread/read`, then commit the local projection. Existing recorded cwd blocks A→B reassignment. Affinity is keyed by canonical thread ID and directory, never by Git origin. `turn/start.cwd`, command or shell `pwd`, explicit inputs, sandbox, approvals, and writable roots remain independent and do not create another Project affinity. Adoption failure remains fail-open and creates no private pending/receipt state. The App does not create managed Worktree/Handoff state. Missing or unavailable Workspace remains fail-open for projectless new task, input, and ordinary Codex conversation. An Agent Package requires Workspace or another managed target only when its owner-projected action lists that context in `required_payload_fields`; the shell never infers this requirement from a manifest. A package failure remains local to that Agent instead of disabling ordinary Codex. | `contracts/app-gui-product-contract.json#interaction_baseline.conversation_scope`, `contracts/app-gui-product-contract.json#agent_package_activation_policy`, `contracts/app-runtime-bridge.json#canonical_conversation_continuity_policy`, `contracts/app-product-profile.json#gui.ordinary_conversation`, `contracts/app-page-state-matrix.json#pages[id=ordinary_conversation]`, active-shell validation, and focused Home/conversation tests. |
| Model selector is visible and Auto follows the CLI default | Home and ordinary Codex conversations show the App model selector. Auto currently resolves to `gpt-5.6-sol` with the App override `max`; future unknown models marked `isDefault` by Codex CLI are accepted even when absent from the known preference list and use the highest CLI-advertised `supportedReasoningEfforts` option. Catalog failure falls back to `gpt-5.6-sol + max`. Auto persists only its mode, while fixed model/reasoning choices persist their concrete override. Backend, provider, and executor selectors stay out of the ordinary path; App-owned model/reasoning and permission/access controls remain visible. | `contracts/app-product-profile.json#codex.auto_model_policy`, `contracts/app-product-profile.json#gui.home.codex_model_display_options`, `contracts/app-gui-product-contract.json#executor_policy`, `contracts/app-page-state-matrix.json#guid_home`, `docs/product/gui/codex-auto-model-policy.md`, active-shell resolver/tests, and Framework installer consumption. |
| Settings/model/reasoning policy is single-sourced by App contracts | Models, Home, ordinary Codex conversations, AionUI, `opl-native-workbench`, Hermes prior-candidate surfaces, WebUI, and the Framework installer must derive Auto resolution, fallback, persistence, visible known models, reasoning options, and access-readiness presentation from the App product profile. The only manually maintained default model/reasoning pair is `codex.auto_model_policy.configured_default`; `npm run codex:model-policy:sync` projects it into compatibility fields and App consumer contracts. Shells and installers render/project this policy only and must not fork a provider/model/reasoning policy or promote release/live evidence into Settings completion. | `contracts/app-settings-control-plane.json#model_reasoning_policy_source`, `contracts/app-product-profile.json#codex.auto_model_policy.configured_default`, `scripts/sync-codex-model-policy.ts`, active-shell validation, and focused release-boundary tests. |
| Conversation pending feedback shows elapsed seconds | Sending a message must immediately expose visible processing feedback with elapsed seconds while ACP/Codex is pending or running, even if thinking/tool events have begun. Raw trace remains diagnostic-only. | `contracts/app-gui-product-contract.json#pages.guid_home.conversation_feedback_policy`, `contracts/app-page-state-matrix.json#guid_home`, `ThoughtDisplay`, `AcpSendBox`, and focused shell tests. |
| First GUID message warms ACP before send | Fresh installs can spend their first ACP startup window resolving the Codex ACP adapter and local runtime state. GUID-created initial messages must await `conversation.warmup` before posting `/messages`, so cold startup happens on the explicit warmup path instead of racing the first user message into the 30s ACP handshake timeout. | Active shell `useAcpInitialMessage`, `warmupConversation`, shell focused tests, and `scripts/validate-active-shell.ts`. |
| Professional agents are packages, not App modules | First-party, organization, user, and third-party packages share one public `app_state.agent_packages.directory.entries` catalog. Framework built-in Release Set is the sole runtime source of the seven canonical first-party identities; the App default registry is external-discovery-only, may be empty, and must have zero canonical-id or first-party-trust collisions. Settings Agents renders every directory entry and consumes its lifecycle/readiness plus exact five-field actions; App profile release-set metadata, `modules.items[]`, runtime-source carriers, and Home shortcuts may enrich labels or diagnostics only and cannot seed a manageable collection. Status-index supplies canonical dependency readiness, repair availability, activation preparation, dependent guards, exposure, action receipt, `rollback_ref`, and physical-surface diagnostics without overriding directory lifecycle or actions. Direct manifest installation requires explicit user-selected trust tier; registry-selected install uses the projected payload. The App and shells never infer checkout installation, ready state, action ids, or payloads. `rollback_ref` remains a displayed recovery reference, while Managed Update owners retain rollback authority. Public publication, installed reload, and live user-path proof remain release/runtime evidence rather than incomplete App product work. | `docs/product/gui/agent-package-management-plan.md`, `contracts/app-runtime-bridge.json#canonical_state_display_action_map`, `contracts/app-gui-product-contract.json#pages.settings_agents.agent_package_lifecycle_ux`, `contracts/app-page-state-matrix.json#pages[id=agents]`, `contracts/agent-package-registry.json`, `contracts/app-product-profile.json#gui.agent_package_registry`, `contracts/app-install-exposure-policy.json#agent_installation_contract.agent_registry_policy`, Framework App-state package projection, and active-shell validation. |
| AI-first interaction is fail-open by default and package failures stay local | The App first tries an owner-projected repair or self-repair, then just-in-time preparation, then a visible degraded mode or safe fallback. Package launch uses `ready / degraded / package_unavailable`: ready and degraded allow send; package_unavailable blocks only the selected package while preserving the draft, plain Codex, other Agents, and existing sessions. A reproducibility or regulated policy may tighten owner-projected requirements but must not create a fourth `strict` state. Optional activation `use_receipt_ref`, binding, closure, stale status, update availability, optional dependencies, and non-required Workspace never become global launch gates; the separate required invocation receipt records only the actual package-shortcut launch fact. Explicit identity/version mismatch, missing entrypoint, unsafe target, permission/authorization denial, unconfirmed irreversible mutation, and unsupported success claims remain locally fail closed. | `contracts/app-gui-product-contract.json#ai_first_interaction_model`, `contracts/app-gui-product-contract.json#agent_package_activation_policy`, `contracts/app-gui-product-contract.json#agent_package_invocation_receipt_policy`, `contracts/agent-package-surfaces.schema.json#/$defs/agent_package_activation_result`, GUI/installation validators, and focused release-boundary tests. |
| Source materials are Framework refs before domain agent work | When a user drops a PDF or file and states a goal, the App route is to call Framework-owned `opl workspace source ingest` and carry `source_material_refs`, `source_material_receipt_refs`, provenance refs, and later `reference_design_packet_refs` through the conversation/task readback. OMA may consume those refs for reference design distillation; MAS, MAG, RCA, and OBF consume source refs through their own domain authority. The App only shows user-friendly file/source/provenance status and passes refs; it does not parse PDF bodies, judge extraction quality, write domain truth, create owner receipts, or claim release/domain readiness. This is App-to-Framework route contract landing, not live drag-and-drop UI evidence. | `contracts/app-runtime-bridge.json#source_material_projection`, `contracts/app-gui-product-contract.json#source_material_user_path`, `contracts/app-page-state-matrix.json#ordinary_conversation`, and focused release-boundary validation. |
| Home shortcuts carry package/profile metadata, not a session contract | The ordinary Home composer should render configured shortcuts over installed OPL Packages that are meant to launch user work. In Settings Agents, shortcut visibility and order are managed inline on the package directory row rather than on a detached second table; Settings Capabilities owns Skills and Plugins. Shortcut metadata may declare label, package id, required Codex skills, source, and refs-only display policy. It must not define domain workflow, MAS stage behavior, artifact schema, prompt internals, readiness verdict, or quality/export authority. OPL Flow is an OPL Package with `package_kind=workflow_plugin_package`, not a Home professional-agent shortcut. Existing `assistant_skill_profiles` are migration inputs toward generic package shortcut metadata. | `docs/product/gui/agent-package-management-plan.md`, `contracts/app-gui-product-contract.json#assistant_skill_profiles`, `contracts/app-product-profile.json#gui.assistant_skill_profiles`, `contracts/app-page-state-matrix.json#guid_home`, active-shell validation, and shell GUI tests. |
| Agent references are prompt content, not selection or engineering authorization | A new session has zero or one active Agent Package. Only a Home starter, the new-session capability palette, or an explicit capability route may change it. Agent names and `@` mentions remain literal prompt content, may coexist without activating more packages, and never authorize OMA to engineer another Agent. Cross-Agent admission is a whole-request semantic judgment owned by the target primary Skill; keywords, regexes, file extensions, validator failures, and `@` mentions cannot act as deterministic routers. | `contracts/app-gui-product-contract.json#ordinary_capability_selector_policy.agent_reference_admission_policy`, `contracts/app-product-profile.json#gui.ordinary_capability_selector_policy.agent_reference_admission_policy`, `docs/product/gui/ideal-interaction-spec.md`, active-shell `GuidPage` / `useGuidMention`, and focused shell tests. |
| Ordinary skill/MCP selection is OPL allowlist only | The ordinary Home composer and ordinary conversation loaded-capability surfaces use App-owned allowlists derived from the active package shortcut and App packaged skill policy. AionUI implementation skills such as `aionui-skills`, `aionui-webui-setup`, `skill-creator`, `cron` when not explicitly allowed by the active package, unlisted MCP servers, and disabled Team MCP snapshots such as `aionui-team`, `team_*`, and `mcp__aionui-team*` are excluded from ordinary selection, loaded-capability display, new conversation create payloads, and inherited session MCP state. | `contracts/app-product-profile.json#gui.ordinary_capability_selector_policy`, `contracts/app-gui-product-contract.json#ordinary_capability_selector_policy`, `contracts/app-page-state-matrix.json#guid_home`, active-shell validation, `GuidPage`, `useGuidSend`, `FileAttachButton`, `ConversationSkillsIndicator`, and shell whitelist tests. |
| Professional agent launch must be observable as an invocation receipt | Selecting a configured package shortcut creates a Codex CLI conversation and persists a launch-only receipt with executor, package/profile or assistant id, required skill ids, source, and launch surface. The active shell validates `opl_agent_package_invocation` in packaged route smoke while preserving the legacy route alias during migration. The receipt proves what the App launched; it does not govern session behavior or prove domain readiness. | `contracts/app-gui-product-contract.json#builtin_assistant_route_receipt_policy`, `contracts/app-product-profile.json#gui.builtin_assistant_route_receipt_policy`, `contracts/app-page-state-matrix.json#guid_home`, `docs/product/gui/agent-package-management-plan.md`, active-shell validation, and shell unit tests. |
| Ordinary Settings navigation 由 App 拥有 | Settings is the App OPL Control Center. The ordinary IA is 概览, 账户与 Gateway, 模型, 工作区与个性化, 智能体, 能力, 资源与连接, 维护, 数据与存储, and 偏好. About is the only independent secondary page; Advanced redirects to Maintenance diagnostics, while Update, Theme, Local Services, and Personalization are compatibility deep links. Account & Gateway alone owns login, identity, balance, usage, Key lifecycle, refresh, repair, and disconnect. Models owns model source and preferences; Resources excludes the built-in Gateway and its count. Workspace & Personalization owns the workspace path, App log directory, user AGENTS.md, and OPL App context; Storage references the log path read-only and owns usage/cleanup; Preferences owns App behavior/display; Maintenance diagnostics owns Framework/raw paths. Desktop log changes use `application.setLogDirectory { path }`: persist `hostLogDir`, switch the live log writer, and roll persistence back with a typed failure if switching fails; the success directory value is only `hostLogDir`, while `cacheDir/workDir` remain unchanged. WebUI `/data/logs` follows the existing `OnePersonLab/data -> /data` volume and Settings never rewires it. First-window rendering consumes a narrow persisted snapshot, never blocks on the complete fast projection or page drilldowns, and is budgeted at 1,500 ms cold/warm with a 262,144-byte startup projection. Each user question uses one bounded OPL card with flat internal rows; normal state stays quiet, exceptions carry emphasis, and technical identifiers remain collapsed. `contracts/app-settings-control-plane.json` owns registry, route, redirect, state/action, startup performance, visual evidence, and completion-audit policy; `settings_ia.v1` remains the App GUI source contract. Changes to ordinary/secondary ownership require contract, matrix, validators, tests, and visual QA to move together. Contract and screenshot evidence do not prove release readiness, installed-App currentness, runtime currentness, launch timing, or owner acceptance. | `docs/product/gui/settings-control-center.md`, `contracts/app-settings-control-plane.json`, `contracts/app-shell-adapter.json#implementation_probes.settings_control_plane_shell_adapter_slot`, `contracts/app-gui-product-contract.json#settings_navigation.settings_ia`, `contracts/app-page-state-matrix.json`, `contracts/app-product-profile.json#settings`, active-shell validation, release-boundary tests, shell Settings tests, and installed-App timing/visual evidence. |
| AionUI Settings fork maintenance uses the existing control plane | Claude's broad fork-maintenance proposal is narrowed to the current repo architecture: enhance the App-owned Settings Control Plane, keep `SettingsHost` / `SettingsShellAdapterSlot` as a thin shell adapter, and require upstream intake to be `accepted`, `adapt`, `redirect`, or `reject` before any registry or slot change. The App does not create a new integration package, Settings plugin ecosystem, overlay topology, or shell-owned product IA to solve fork maintenance; numeric ROI claims are excluded unless backed by fresh evidence. Shell delta remains renderer, bridge, route/slot, styling/i18n, and focused test evidence only. | `docs/architecture/opl-aionui-fork-maintenance-strategy.md`, `docs/product/gui/settings-control-center.md`, `contracts/app-settings-control-plane.json`, `contracts/app-gui-product-contract.json#product_authority`, and `contracts/app-shell-adapter.json#implementation_probes.settings_control_plane_shell_adapter_slot`. |
| Home minimal command surface and Settings IA require matrix parity | Home is composer-first and does not show runtime activity, continue-work, per-assistant running badges, or footer quick icons. The ten ordinary Settings pages, About secondary page, Advanced-to-Maintenance diagnostics redirect, startup snapshot budget, and lazy drilldown policy must be declared consistently in the GUI product contract, Settings control plane, product profile, and page-state matrix. | `contracts/app-gui-product-contract.json#pages`, `contracts/app-settings-control-plane.json`, `contracts/app-page-state-matrix.json`, `contracts/app-product-profile.json#settings`, `scripts/validate-active-shell.ts`, and focused release-boundary GUI tests. |
| Skill is the public ABI; plugin is the App distribution shell | Professional agents remain domain skills/plugins backed by their own action/stage metadata source. Codex App can expose them through plugin-packaged skills, while CLI/direct Codex paths use the same skill semantics. App install policy must not turn plugin-packaged domain skills into duplicate companion skill mirrors or App-owned session behavior contracts. | `contracts/app-install-exposure-policy.json`, `contracts/app-product-profile.json#companion_payloads`, OPL Framework `opl connect sync-skills`, `docs/product/gui/agent-package-management-plan.md`, and active-shell validation. |
| Codex configuration syncs App-visible OPL family plugins | Fresh Full installs must not require a second manual Codex plugin setup after the API key is configured. `opl system configure-codex --api-key-stdin` consumes the Full runtime module paths, syncs MAS/MAG/RCA plugin-packaged skills, generates the OMA and BookForge local plugin surfaces, and writes the Codex plugin registry in the same setup step. | OPL Framework `system configure-codex`, `syncFamilySkillPacks`, App Full runtime wrappers, and release-boundary wrapper validation. |
| Codex fallback uses an offline archive wrapper | The Full DMG remains self-contained on a clean machine without carrying the expanded Codex vendor binary directly in `runtime/current/bin`. The packaged fallback is `runtime/current/bin/codex` as a shell wrapper plus `runtime/current/vendor/codex/codex_cli_darwin_arm64.tar.gz`; `--version` stays fast and real Codex execution extracts the archive into the installed runtime cache before exec. | `contracts/app-release-channel.json#full_first_install.required_payloads.codex_cli`, `scripts/build-full-first-install-package/runtime-layers.ts`, `scripts/verify-remote-release-assets.ts`, and Full clean-VM smoke evidence. |
| First conversation uses capability-specific prerequisites before ACP warmup | First-run Core readiness still combines workspace root, Codex CLI, and usable model access, but it does not block `/guid`. Plain conversation send and current-session attachment/file/directory/paste/drop/`/open` inputs require Codex CLI and model access only; workspace readiness gates only project selection and OPL workspace controls. A blocked send preserves the draft and shows an inline non-modal route back to FirstRun. After prerequisites pass, the conversation record and ACP warmup still complete before `sendMessage`, so slow dependency unpacking does not become a fixed handshake failure. Domain modules, Codex plugin sync, and Temporal provider readiness stay visible background maintenance. | `contracts/app-product-profile.json#first_run.first_conversation`, `contracts/app-product-profile.json#first_run.ordinary_shell_recovery`, `contracts/app-gui-product-contract.json#first_launch_readiness_policy.ordinary_shell_recovery_policy`, active-shell Guid/Sider implementation, and focused shell tests. |
| Incomplete first-run remains recoverable from the ordinary shell | After a user explicitly enters `/guid`, incomplete Core readiness is shown as a quiet persistent sidebar entry rather than a modal or route gate. The entry returns to `/first-run` only when clicked. Unknown readiness does not synthesize failure or mutate `ready_to_launch`. | `contracts/app-gui-product-contract.json#first_launch_readiness_policy.ordinary_shell_recovery_policy`, `contracts/app-page-state-matrix.json#first_launch_readiness`, `contracts/app-first-run-test-matrix.json#ordinary_shell_progressive_first_run_recovery`, and active-shell DOM tests. |
| Desktop first-run defaults to OPL Gateway account login | When Codex model access is not configured, Desktop offers Gateway account login first with email and password; API Key remains a compatibility method, and existing Codex detection is a separate secondary action. Passwords use the desktop-only typed stdin bridge, are cleared after success, failure, or method switch, and never enter App state or diagnostics. After login, the App reads fast state and completes setup only for a uniquely resolved Codex group. WebUI exposes API Key only. | `contracts/app-gui-product-contract.json#first_launch_readiness_policy.beginner_presentation.model_access_setup`, `contracts/app-runtime-bridge.json#opl_gateway_account_secret_bridge`, `contracts/app-page-state-matrix.json#first_launch_readiness`, `contracts/app-first-run-test-matrix.json#beginner_simplified_first_run_clean_machine`, and active-shell FirstRun tests. |
| First-run preparation is visible but skippable after Codex core is available | A first-run checklist may carry real OPL preparation tasks because it gives users visible progress, but non-core/network-dependent preparation must not trap users before the App. Once Codex CLI / Codex adapter can start and any usable Codex model access exists, the user can skip OPL Gateway first-launch configuration into the chat-first shell; the skip writes a `user_deferred` routing marker and must not claim OPL Gateway API key configured, module reconcile complete, MAS/MAG/RCA domain ready, or Full OPL readiness. Settings must keep an OPL Gateway configuration entry so users can switch later. AionUI and foreground candidates may adopt this pattern only through their adapter contract and release-shell validation. | `docs/product/gui/opl-native-workbench-plan.md`, `docs/product/shell-alternatives/hermes-first-run-flow.md`, `contracts/shell-adapters/opl-native-workbench.json`, candidate first-run contracts, and focused candidate tests. |
| Temporal defaults are App-configured and OPL-owned | The App may configure default Temporal environment for the packaged OPL runtime, but it does not own provider implementation or runtime truth. Full runtime wrappers export `OPL_FAMILY_RUNTIME_PROVIDER=temporal`, `OPL_TEMPORAL_ADDRESS=127.0.0.1:7233`, `OPL_TEMPORAL_NAMESPACE=default`, and `OPL_TEMPORAL_TASK_QUEUE=opl-stage-attempts`; the bundled Temporal CLI fallback is an offline archive wrapper over `runtime/current/vendor/temporal/temporal_cli_darwin_arm64.tar.gz`, extracting into the installed runtime cache only when Temporal is actually executed. On Desktop macOS, OPL Framework owns a login-resident Temporal server supervisor, stable launcher binding, persistent SQLite under the OPL state root, Server -> Worker -> Scheduler startup reconciliation, service/worker/scheduler actions, residency proof, lifecycle diagnostics, and repair receipts. WebUI/container, explicit external Temporal, and non-macOS foreground services use their platform lifecycle and report the LaunchAgent supervisor as not applicable. Migration from an already-running in-memory development server may adopt the process role but cannot claim prior workflow-history migration. | `contracts/app-install-exposure-policy.json#temporal_auto_configuration`, `contracts/app-release-channel.json#full_first_install.required_payloads.temporal_runtime_provider`, `scripts/full-first-install-runtime-wrappers.ts`, `scripts/verify-full-runtime-native-trust.ts`, and OPL Framework family-runtime commands. |
| App software lifecycle has exactly three user-facing objects | Ordinary users see OPL Base, OPL App, and OPL Packages. `managed_update.components` admits only `opl_base`, `opl_app`, and `opl_packages`; legacy component mappings and the ordinary component picker are removed. Base dependency/integration status, Packages projection/profile migration status, and App host-route status are nested details rather than peer products or updaters. | `contracts/app-release-channel.json#managed_update_plane.software_lifecycle`, `contracts/app-install-exposure-policy.json#software_lifecycle`, `contracts/app-product-profile.json#install_update_taxonomy`, and focused release-boundary tests. |
| Base dependencies and companion integrations are internal status | Codex/Temporal/Framework/runtime dependencies plus OfficeCLI/MinerU integration may be packaged and diagnosed, but they appear only under OPL Base `dependency_status` or `integration_status`. The App may request Framework bootstrap and show receipts; it does not own Base mutation or publish a separate Runtime/Companion updater. | `contracts/app-release-channel.json#managed_update_plane.software_lifecycle.objects.opl_base`, `contracts/app-install-exposure-policy.json#exposure_classes.opl_base_payloads`, and `scripts/validate-active-shell/release-contract-validator.ts`. |
| OPL Base artifact evidence remains an App release gate without transferring authority | Release validation may require Base artifact ref/checksum/rollback evidence for the exact cohort. The App consumes those refs only; Framework owns Base lifecycle, and Docker/WebUI image replacement remains an App carrier host route. | `contracts/app-release-channel.json#managed_update_plane`, release-boundary tests, and Framework lifecycle receipts. |
| Runtime/resource/task/data lifecycle refs use one owner matrix | OPL Runtime Payload / Fabric, Environment Materializer, TaskRunProjection v2, OPL Fabric resource refs, local data lifecycle, Aion shell rendering, and Homebrew distribution are kept in one matrix so new refs do not become competing state machines. App owns user-facing policy and release gates, OPL Framework owns family projections and runtime receipts, Aion renders as a thin consumer, and Homebrew mirrors release cohorts only. | `contracts/app-runtime-bridge.json#runtime_surface_owner_matrix`, `contracts/app-runtime-bridge.json#task_awareness_projection`, `contracts/app-release-channel.json#local_data_lifecycle`, active-shell validation, and release-boundary tests. |
| OPL Packages use one Framework lifecycle | MAS/MAG/RCA/OMA/BookForge/MAS Scholar Skills/OPL Flow are OPL Packages. Ordinary App operations route to the canonical `opl packages` install/update/repair/uninstall surface. Codex registry/reload is nested `projection_status`; profile semantic merge is nested `profile_migration_status`; neither Homebrew nor the App owns Package mutation. | `contracts/app-install-exposure-policy.json#agent_installation_contract.managed_package_distribution`, `contracts/app-release-channel.json#managed_update_plane.software_lifecycle.objects.opl_packages`, and Framework `opl packages` behavior tests. |
| Every App carrier converges through one Framework reconciliation | Standard updater, DMG/installer, Homebrew Cask, Full, WebUI, and future supported App carriers only provide candidate bytes or seeds. On first launch or a changed running App version/image checkpoint, the App first publishes a valid read-only Framework `status` projection, then requests generic Framework `check -> plan -> optional apply -> status` for Base and Packages. The prefetch failure does not block reconciliation or clear the last valid projection. Silent apply requires Framework plan `auto_apply.eligible`, `app_background_safe`, and `command_ref`; after an apply, the App preserves its receipt and performs a terminal status readback before committing the checkpoint, so Settings consumes current Framework state rather than an incomplete mutation receipt. The App never owns a second update catalog. A successful no-op readback may commit the checkpoint without inventing a lifecycle receipt. OPL Flow profile migration is a normal package post-apply hook, not an App-update special case. | `contracts/app-release-channel.json#managed_update_plane.carrier_reconciliation`, `contracts/app-install-exposure-policy.json#software_lifecycle.three_layer_contract`, `contracts/app-gui-product-contract.json#framework_surfaces.managed_update_plane`, and active-shell validation. |
| Agents owns Agent Package lifecycle; Maintenance owns shared software currentness | Agents carries the public package directory, lifecycle actions, developer source, dependency/activation readiness, and Home visibility. Maintenance carries OPL Base/App/Packages currentness, managed dependency status, shared update/repair/rollback operations, local services, and read-only diagnostics. Neither page implements the update kernel, silently mutates developer or dirty checkouts, treats Codex Surface as a second package channel, or writes runtime/domain truth. | `contracts/app-gui-product-contract.json#pages.settings_agents`, `contracts/app-gui-product-contract.json#pages.settings_environment`, `contracts/app-settings-control-plane.json#agents_capabilities_ownership`, `contracts/app-page-state-matrix.json`, and focused Settings validators. |
| Local data cleanup is inventory-first and artifact-safe | Updater installer packages, conversation artifacts, OPL Runtime Fabric roots, and logs are separate lifecycle classes. The App may clean stale updater installer cache packages, but conversation workdirs and generated deliverables require inventory, archive/export, restore proof, and explicit user confirmation before deletion. OPL Runtime Fabric prune follows current/rollback pointers and starts as dry-run; logs use bounded rotation and cannot stand in for artifact cleanup evidence. | `contracts/app-release-channel.json#local_data_lifecycle`, active-shell validation, `shells/aionui/packages/desktop/src/process/services/localDataLifecycle/index.ts`, focused shell lifecycle tests, and historical Issue #5 provenance in `docs/history/process/local-data-lifecycle-issue-5.md`. |
| Storage consumes owner projections instead of becoming a Docker cockpit | The ordinary Storage page keeps the existing Shell-owned updater/conversation/runtime/log categories and adds only `agent_package_store` and `webui_data_volume` from canonical owner projections. It does not inventory generic Docker images, containers, volumes, or build cache. Missing owner projections and unknown bytes fail open without synthetic zero. Package lifecycle remains on Agents. WebUI mutation is available only when the carrier host injects the complete authenticated plan/execute/restore ABI; otherwise the row remains status-only. Requests carry only opaque plan/hash/confirmation/receipt refs, execution returns archive/manifest/receipt refs and a fresh terminal readback, and errors never expose raw paths. Shell may display and invoke projected actions but may not scan owner roots, delete paths, synthesize receipts, or run generic Docker prune. | `contracts/app-release-channel.json#local_data_lifecycle.owner_storage_projections`, `contracts/app-gui-product-contract.json#pages.settings_storage.owner_storage_projections`, `contracts/app-settings-control-plane.json`, and P5 focused validators/tests. |
| OPL Packages channel failures fail closed with packaged fallbacks | A missing or unavailable GitHub Packages/GHCR OCI OPL Package `latest-stable` tag or digest is a package-channel failure, not a reason to hand-install a divergent OMA, MAS Scholar Skills, or OPL Flow surface. The source order is bundled Full runtime modules, Framework-managed GHCR OPL Packages `latest-stable`, then explicit Developer Profile checkout source. Git/local checkout sources remain development inputs and are never ordinary managed lifecycle state. | `contracts/app-install-exposure-policy.json#agent_installation_contract.managed_package_distribution`, `npm run validate:agent-installation`, Full runtime module path wrappers, and Framework `opl packages`. |
| App packaging consumes OPL Flow dependency policy | `default_packaged_codex_skill_ids` and `additional_package_skill_ids` contain App/domain package entries only. OfficeCLI, MinerU, UI UX Pro Max, their tools, conflicts, and model recommendation come from OPL Flow `workflow-policy.json`. Full resolves `requires + recommends` with `offline_bundle=full`; the shell receives a generated compatibility projection, not a second policy owner. | `contracts/app-product-profile.json#companion_payloads`, OPL Flow `contracts/workflow-policy.json`, Full first-install package scripts, and active-shell validation. |
| User instructions and App session context have separate owners | Codex reads the user-owned `$CODEX_HOME/AGENTS.md` and repository `AGENTS.md` independently. Settings may edit the user file only through the Framework action boundary with SHA stale-write protection, backup, and atomic replacement, and may restore the canonical profile from the selected current OPL Flow package. OPL App separately owns a concise new-conversation context generated from professional Agent Package routing summaries; users may append or clear additional instructions but cannot replace the generated directory. OPL Flow owns the user profile default but not the App session prompt. | `contracts/app-product-profile.json#codex`, `contracts/app-gui-product-contract.json#opl_app_session_context`, `contracts/app-settings-control-plane.json#configuration_catalog_projection`, Framework `app_state.codex_personalization` / personalization actions, and active-shell Preferences DOM tests. |
| Settings builtin skills are filtered through the App visibility policy | The active shell may report upstream builtin, installed, and auto-injected skills, but ordinary Capabilities shows only `default_visible_skills`. Packaged companion skills remain available to Codex task routing without becoming ordinary App menu entries. AionUI helper skills such as `aionui-skills` do not surface as OPL App capabilities. | `contracts/app-gui-product-contract.json#pages.settings_capabilities`, `contracts/app-page-state-matrix.json#capabilities`, `contracts/app-product-profile.json#codex.default_visible_skills`, `SkillsHubSettings`, active-shell validation, and shell tests. |
| Active shell stays an external implementation carrier | `shells/aionui/` is the current active shell checkout from `gaofeng21cn/opl-aion-shell`; shell history and implementation truth stay in the shell repo. The upstream AionUI fork body is read-only by default: App work may touch only App contracts, adapters, OPL overlays, packaging/readback hooks, and validation for those surfaces. | `contracts/app-shell-adapter.json` and active-shell validation decide whether a shell satisfies the App contract. |
| Active shell upstream intake is classified before release | Broad AionUI intake uses fail-closed `absorbed`, `rejected`, or `deferred` records for every validator-owned required capability and dependency. The v2.1.31 intake distinguishes fork base `70974c59`, evaluated upstream `e49cd949`, and selective absorption head `e38b00ba`; startup directories, database recovery, feedback privacy, Cron, `/guid` allowlist, and Chinese/English Settings/i18n refinements are absorbed, while non-Chinese/English locales and Team are rejected. The validator reads the resolved active shell package, fixes the AionCore minimum at `v0.1.44`, and requires selective absorption and remediation refs to be ancestors of shell `HEAD` rather than permanent exact-HEAD pins. Recovery admission accepts `BOOTSTRAP_DATA_INIT_FAILED` at `database.recoverable_corruption`, or at `database.open` only with a strict SQLite corruption marker; success must emit `BOOTSTRAP_RECOVERED_DATABASE_CORRUPTION` at `database.recovery`. The AionCore remediation ancestor is `81c8b37fd`; ordinary open, lock, and permission failures remain generic. Settings-specific registry/slot intake separately keeps fixed `accepted`, `adapt`, `redirect`, or `reject` buckets. | `contracts/app-shell-adapter.json#upstream_intake`, `scripts/validate-active-shell/upstream-intake-policy-validator.ts`, `tests/release/app-release-boundary-cases/upstream-intake-policy-validation.test.ts`, `docs/architecture/opl-aionui-fork-maintenance-strategy.md`, `contracts/app-settings-control-plane.json#upstream_intake_classification`, and active-shell validation. |
| Retained X0-01 Runtime route is WorkItemProjection V2 first | When explicitly enabled, the conditionally retained Runtime route consumes Framework-produced `WorkItemProjection v2`, not raw attempts or a Shell-built union. Scope is Agent -> Project; work items remain rows and the status filter does not duplicate scope. Each canonical work item appears once in a four-column list: project/work item, status, progress/next step, elapsed/Token, with the agent as a secondary label. System attention requires a complete current responsibility envelope, missing Token never renders as zero, and unconfigured limits never produce progress bars. Stage opens a lightweight complete Stage map with current/next Stage and current Attempt; task detail remains limited to Work Item, Stage/Attempt, elapsed/heartbeat, Token, and read-only next-action semantics. This source-retained route is not a core requirement, default release gate, or Native phase-1 parity target; its route detail is an explicit optional gate while the Framework producer remains required. | `contracts/app-runtime-bridge.json#work_item_projection`, `contracts/app-page-state-matrix.json#pages[id=runtime]`, `contracts/app-gui-product-contract.json#pages.runtime_status.runtime_cockpit_product_contract`, and OPL Framework/Shell/live evidence owned by their respective lanes. |
| Desktop/WebUI are current; Workspace/Fabric/Console are conditional X0 roles | OPL App and U1-05 Docker/WebUI are the current local/browser workbench surfaces. Hosted OPL Workspace is X0-03 and appears only with real account, storage, isolation, backend, and owner policy. OPL Gateway remains the AI access surface. OPL Fabric and Console are X0-04 retained refs/owner routes shown only when their real backends exist; ordinary Settings maintains no placeholder status, scheduling, billing, credentials, or execution truth. User-provided local, SSH, or HPC resources remain self-managed unless Framework projections mark them as Console-managed. | `contracts/app-runtime-bridge.json#task_awareness_projection.resource_context_policy`, `contracts/app-gui-product-contract.json#pages.settings_access.cloud_fabric_surface`, `contracts/app-settings-control-plane.json#slot_registry.settings_access`, `contracts/app-page-state-matrix.json#pages`, and `docs/architecture.md`. |
| Claude Science learning lands through current-task awareness | Claude Science is treated as an external workflow pattern source, not as a new App dashboard or shell-owned task system. Chat renders the core current-task context and Inspector renders artifact provenance; the X0-01 Runtime route may render a cross-project slice only when enabled. Settings and conversation owner surfaces host action controls. OPL Framework/domain producers own task, artifact, reviewer, workflow, connector, and action receipt refs; the App repo owns display contracts and validation; AionUI remains a thin consumer. | Historical reference `docs/product/gui/claude-science-runtime-task-awareness-plan.md`, `contracts/app-runtime-bridge.json`, `contracts/app-page-state-matrix.json`, `contracts/app-gui-product-contract.json#pages.runtime_status`, OPL Framework `opl app state` / `opl app action`, and active-shell focused tests. |
| OpenScience accepted items are refs-only App projections | The four accepted OpenScience items are product projection boundaries: structured result panels live inside conversation/current-task/right inspector, artifact/provenance appears as refs-only card/drawer, ref-level comments become review/request-change/follow-up prompt/action refs without an App annotation store, and workflow/skill candidates are report-first Settings / Capabilities refs with review, needs changes, and continue in conversation actions. OPL App remains the shell-wrapped Codex App, and professional agents are Codex plugins or packaged Codex skill surfaces. | `contracts/app-runtime-bridge.json#structured_result_panel_projection`, `contracts/app-runtime-bridge.json#artifact_provenance_bundle_projection`, `contracts/app-runtime-bridge.json#ref_level_follow_up_projection`, `contracts/app-runtime-bridge.json#workflow_skill_candidate_projection`, `contracts/app-page-state-matrix.json`, `contracts/app-gui-product-contract.json`, `contracts/fixtures/opl-app-state-fast.fixture.json`, and focused release-boundary tests. |
| Provider/platform repair and updates belong to Maintenance | Provider liveness, Temporal/worker readiness, platform repair, Codex/App update, reinstall, and maintenance actions are maintenance operations, not task progress. Maintenance may present typed causes, impact, preview, confirmation, execution, and receipts without changing a Work Item's domain status or claiming domain/release readiness. Runtime may show only the resulting user-facing Work Item status and next-step text. | `contracts/app-gui-product-contract.json#framework_surfaces`, `contracts/app-page-state-matrix.json`, `contracts/app-runtime-bridge.json`, Maintenance validation, and release-boundary tests. |
| Raw diagnostics, State Index, and operator drilldown belong to Maintenance diagnostics | Full App state, operator drilldown, State Index/SQLite refs, raw IDs, logs, command refs, safe-action catalog, and technical receipts are read-only diagnostic inputs under Maintenance. Advanced is a compatibility redirect only. These inputs are absent from Runtime, read-only unless a separately authorized owner action applies, and cannot authorize runtime/domain/artifact/release readiness. | `contracts/app-gui-product-contract.json#framework_surfaces`, `contracts/app-page-state-matrix.json`, `contracts/app-runtime-bridge.json`, Maintenance diagnostics validation, and OPL Framework projected read models. |
| Artifact provenance belongs to Inspector | Current pointer, canonical/export artifact refs, lineage, retention, conformance, manifest/hash/receipt/blocker refs, and artifact previews are task/conversation Inspector content. Runtime does not provide artifact drilldown. The App remains refs-only and does not own artifact bodies, artifact authority, or domain quality/export verdicts. | `contracts/app-gui-product-contract.json#framework_surfaces`, `contracts/app-runtime-bridge.json`, Inspector page-state validation, and OPL Framework Stage Artifact projections. |
| AionUI Team is hidden in ordinary OPL App | Upstream AionUI Team is configured around shell-local team leaders and agents, so it is not an OPL ordinary-user capability. The active shell keeps Team mode disabled, hides the Team sidebar entry, no-ops Team-created redirects, rejects Team deep links, redirects compatible `/team/*` routes back to the App-owned home path, and scrubs disabled Team MCP state from ordinary conversations before rendering or inheriting it. | `contracts/app-gui-product-contract.json#settings_navigation.team_surface_policy`, `contracts/app-gui-product-contract.json#ordinary_capability_selector_policy`, `contracts/app-page-state-matrix.json`, `scripts/validate-active-shell.ts`, and active shell `TEAM_MODE_ENABLED` plus ordinary conversation Team MCP scrub tests. |
| Codex subagents stay separate from AionUI Team | Codex delegated execution, App Server metadata intake, canonical thread discovery, and ordinary read-only Active/Done/detail/result/open-thread UI are source-implemented through the Codex runtime and the single existing adapter. Unknown metadata falls back to the generic tool row, and task-open failure keeps the current conversation usable. Pixel, Install, and Release remain unverified. Do not add a second App Server client, Team store, scheduler, shell-owned execution authority, or mandatory bespoke controls. | `contracts/app-gui-product-contract.json#ordinary_conversation.codex_subagent_activity`, `contracts/app-page-state-matrix.json#ordinary_conversation`, `docs/product/gui/feature-inventory.md`, `docs/product/gui/shell-conformance-matrix.md#b0-11-codex-subagent-证据`, Codex App Server/ACP source evidence, and focused shell tests. |
| Release/user-path evidence is cohort-bound and release-tooling-owned | Release tooling owns the complete same-cohort bundle: Runtime screenshots may be one visual artifact, while full drilldown, action receipts, VM/installed-App smoke, remote verification, and manifests remain release evidence rather than Runtime UI. Draft bundles, verified refs, updater assets, screenshots, and packaged route smoke do not prove stable/latest promotion, MAS/MAG/RCA/BookForge readiness, or OPL family production readiness. | `contracts/app-release-channel.json`, release evidence collectors/manifests, remote release verification, and release owner promotion records. |
| Developer source selection and safe repository maintenance are independent and package-aware | Ordinary users stay on OPL-managed package roots unless the Agents source selector resolves an authorized developer checkout. Settings > Agents keeps the global Managed / Automatic / Developer source choice separate from **Maintain authorized development repositories**, whose user setting is `auto\|off` and defaults to `auto`. A matching developer identity plus successful full repository-authority inspection activates `developer_apply_safe` for authorized repositories and displays effective state, configuration source, GitHub login, repository scope, and dirty-worktree/branch protection; fast state may report inspection pending without claiming the mode is active. Changing maintenance permission never changes the selected checkout, and changing source never grants mutation permission. Each package detail retains Auto / Managed / Developer source selection plus actual, managed, and developer paths. Shared runtime mutation still requires `enabled=on + mode=developer_apply_safe + source=user_config`. Raw `OPL_MODULE_SOURCE_MODE` remains a low-level diagnostic/CI override. | `contracts/app-gui-product-contract.json#pages.settings_agents.developer_mode_control`, `contracts/app-settings-control-plane.json#experience_contract.page_contracts.agents.developer_mode_surface`, `contracts/app-page-state-matrix.json#agents`, `contracts/app-product-profile.json#settings.developer_profile`, Framework developer-mode projection, and active-shell validation. |
| OPL App is the optional GUI/control plane over Framework-owned Base and Packages | Homebrew Formula `opl` and `opl-install.sh --headless --skip-packages` are Base carrier adapters; Cask and signed installer/DMG are App carrier adapters. A missing Base may be bootstrapped from the App through the canonical installer route, but execution and receipt ownership remain Framework-owned. The App mutates only its own binary; Packages use `opl packages`. Carrier choice never creates a second lifecycle truth. | `contracts/app-install-exposure-policy.json#software_lifecycle`, `contracts/app-release-channel.json#managed_update_plane.software_lifecycle`, `docs/architecture.md`, and `docs/delivery/release/README.md#install-and-update-taxonomy`. |
| WebUI uses the same chat-first workbench as desktop | WebUI is how the same OPL App workbench runs in a browser on Linux, Windows, servers, or cloud VMs. It must keep the same chat-first task flow, project/artifact/progress/receipt language, and OPL state/action boundary as the desktop App; it is not a second product profile, runtime source, provider selector, memory store, artifact store, release channel, or full workbench first screen. Current foreground candidate work should apply this through `opl-native-workbench` only when its WebUI delivery surface is explicitly claimed; Hermes WebUI remains prior-candidate reference material, and AGUI WebUI evidence remains archived technical proof. | `contracts/app-shell-candidates.json#alternative_gui_policy`, `docs/product/gui/feature-inventory.md`, `docs/product/gui/opl-native-workbench-plan.md`, and candidate-specific smoke evidence when explicitly claimed. |
| Docker/WebUI beginner path is a non-live functional path before release evidence | The beginner WebUI path is one-click installer first, browser Access API key entry, runtime proxy smoke through `opl system configure-codex --api-key-stdin --json`, startup diagnostics, data/project preservation, host-side image update route, and ordinary user progress after WebUI opens. `/settings/storage` is the same product route on Desktop and WebUI, but carrier capabilities differ: Desktop alone invokes Electron local lifecycle; WebUI consumes valid owner projections and omits missing sections. `host_action_required` remains status-only until the authenticated carrier host supplies the complete bounded plan/execute/restore ABI, at which point only the owner-defined CTA becomes available. Local and optional remote image digest comparison may show `current` or `update_available`, but it is status guidance only and does not prove release-ready, live latest/currentness, applied host update, Pixel, or installed route behavior. | `contracts/app-gui-product-contract.json#pages.settings_storage.storage_carrier_behavior`, `contracts/app-release-channel.json#local_data_lifecycle.storage_carrier_behavior`, `scripts/install-docker-webui.sh`, `scripts/install-docker-webui.ps1`, `scripts/docker-webui-smoke-gate.ts`, and Shell browser-mode/core-route tests. |
| Docker/WebUI cloud deployment fails closed on external secrets | Local beginner Docker/WebUI keeps browser auto-login and browser-first access-key entry. Server/cloud deployment is an explicit separate path: `deploy/docker-webui/cloud` uses Caddy/TLS, Docker secrets, default username `opl`, required WebUI password, optional Gateway API key overlay, and disabled auto-login. Any cloud/password mode or Gateway API key secret without a WebUI password secret must refuse startup; Gateway API key setup remains stdin-only through the local runtime proxy and does not replace WebUI login. This is a structural/security landing, not proof that a live cloud endpoint is deployed or release-ready. | `contracts/app-install-exposure-policy.json#installer_surfaces.docker_webui.installer_model.cloud_deployment_model`, `deploy/docker-webui/cloud`, `scripts/install-docker-webui.sh --cloud-template`, active shell `packages/web-cli/src/deploymentAuth.ts`, `resources/opl-webui-entrypoint.sh`, and focused Docker/WebUI tests. |
| Release efficiency target is build-once/promote-many | A stable release freezes one App/Shell/Framework cohort, builds and qualifies artifacts once, and promotes from a ready candidate record plus owner receipt. The release cohort manifest is the retry entrypoint for rerunning failed gates, VM diagnostics, or promote, not a new source of release truth. Public release notes are prepared as AI-written notes before publish/promote; publish/promote jobs must consume prepared files and must not call AI to generate notes. Full runtime bundle preparation belongs to OPL Framework and is consumed by the App as manifest/lock/readback refs. VM smoke qualifies the exact same-cohort artifact rather than rebuilding or creating missing release material. Target timing is standard 10-20 minutes, Full 35-50 minutes, same-cohort retry 3-15 minutes, and promote under 5 minutes. | `contracts/app-release-channel.json#release_acceleration`, `contracts/app-release-channel.json#public_release_notes`, `contracts/app-release-channel.json#full_first_install.opl_runtime_bundle_consumer`, `docs/delivery/release/README.md#release-efficiency-target-architecture`, `scripts/README.md`, release cohort plan/manifest artifacts, release readiness summary, candidate record, VM smoke artifacts, and promote workflow output. |
| Release execution shares one immutable byte binding | Local and GitHub are executors of the same frozen source and artifact receipts, not separate build authorities. The read-only Bundle reuses the existing build-cohort and qualification receipts to bind one exact App/Shell/Framework cohort, prepared AI notes, Standard public assets, and an optional same-cohort Full add-on. It does not invent toolchain/provenance authority or claim release readiness; Stable only records that the channel permits promotion, while the release controller and owner receipts decide readiness. Full cannot enter updater metadata and Nightly cannot become Latest. | `contracts/app-release-bundle.schema.json`, `scripts/release-bundle.ts`, focused Release Bundle tests in the existing release-boundary lane, and `scripts/README.md#immutable-release-bundle`. |
| GitHub Actions cache is bounded content-addressed acceleration with one writer | The 10 GiB repository allowance reserves 2 GiB headroom and budgets dependency downloads, first-run install seed, compiled output, and Full runtime layers separately. Standard and Full share canonical Bun/Electron download keys instead of writing duplicate lane-prefixed entries. Large caches use explicit restore plus `main`-only miss/contracted-rebuild save; all other refs are restore-only. Exact-only compiled/runtime keys never use prefix fallback. Full runtime cache v2 scopes invalidation to Toolchain, Domain, OPL runtime, or Skills inputs; binds the Framework seven-package owner/manifest/payload identity and each structured layer-input digest before materialization; and emits a receipt only after currentness passes. Receipt hit ratio, duration, and save-failure metrics guide acceleration tuning but never prove artifact identity or release readiness. Cleanup protects current-main-reachable, frozen, rollback, and active keys, but may remove exact stale historical `main` generations. | `contracts/app-actions-cache-catalog.json`, `contracts/app-release-channel.json#release_acceleration.github_actions.cache_policy`, `docs/delivery/actions-cache-architecture.md`, `.github/workflows/full-runtime-cache-warmup.yml`, `.github/workflows/full-first-install-release.yml`, `scripts/write-actions-cache-plan.ts`, `scripts/validate-release-boundary/actions-cache-policy.ts`, and cache identity/plan tests. |
| Standard Stable readiness is decoupled from add-on gates by default | Standard Stable readiness admits from standard publish, standard remote verification, the standard VM gate, and one-shot installer smoke. Full, Docker/WebUI, and Homebrew remain same-cohort add-on gates/assets whose status is recorded in `release-addon-readiness-summary`, but they do not delay the Standard readiness record. | `.github/workflows/desktop-release.yml#release-readiness-admission`, `.github/workflows/desktop-release.yml#release-addon-readiness-summary`, `contracts/app-release-channel.json#release_acceleration.target_architecture`, `contracts/app-release-channel.json#release_acceleration.github_actions.release_readiness_admission`, and release readiness job results. |
| OPL native workbench candidate follows an App-owned structure route | `opl-native-workbench` is the foreground alternative candidate, not the active release shell. The first route is candidate registration, adapter contract, independent external checkout, state/action bridge, shared renderer, package manifest, source/WebUI smoke when claimed, docs/runbook, and later visual/live evidence. K-Dense, OpenClaudeScience / Claude Science, and AGUI lessons are classified as `adopt`, `adapt`, `watch_only`, or `reject`; external runtime/agent authority, Pi/DeepAgents/LangGraph-like runtimes, provider/backend marketplaces, and domain truth ownership are not App or shell authority. Hermes remains a retained reference candidate, not the default foreground scope. | `docs/product/gui/opl-native-workbench-plan.md`, `docs/product/gui/kdense-byok-learning-landing.md`, `docs/product/gui/claude-science-runtime-task-awareness-plan.md`, `docs/active/app-ideal-state-gap-plan.md#opl_native_workbench_candidate_gate`, `contracts/app-shell-candidates.json`, and `contracts/shell-adapters/opl-native-workbench.json`. |
| External UI projects can inform information organization without becoming authority | PilotDeck is a reference for optional workspace/session rail, chat-first main pane, grouped files, memory, routing, and always-on context. Its AGPL source and runtime are excluded unless a separate license and candidate-adoption decision is made, and it is not a first-screen workbench template. | `contracts/app-shell-candidates.json#design_references` and `docs/product/gui/feature-inventory.md#PilotDeck-Informed Information Organization`. |
| Standard updater and Full first-install assets stay separate | Standard updater metadata must never select `Full` assets. Full first-install evidence remains a separate release gate and must not be promoted from missing artifacts. | `contracts/app-release-channel.json`, release evidence manifests, remote release verification, and first-run VM evidence. |
| Full runtime executables must not require separate Gatekeeper approval | A Full install must not ask users to approve the App and then approve packaged runtime executables again. Distributable Full builds must sign native executables carried inside the runtime payload, verify the App bundle through `spctl`, verify packaged runtime native trust through `full-runtime-native-trust.json`, and upload that evidence with the Full release assets. Temporal CLI is packaged as an offline archive wrapper; clean VM smoke covers archive extraction and execution without a second user approval. | `scripts/build-full-first-install-package.ts`, `scripts/verify-full-runtime-native-trust.ts`, `.github/workflows/full-first-install-release.yml`, `contracts/app-release-channel.json`, and `scripts/verify-remote-release-assets.ts`. |
| Full size review cannot weaken offline first-install completeness | The Full package keeps the clean-machine offline first-install promise even when the compressed DMG exceeds the review threshold. The `750000000`-byte Full DMG threshold opens size review and optimization planning; it is not release-blocking by size alone and does not authorize removing required runtime, shell, native trust, or Core readiness payloads. Full optimization writes `package_optimization`, `full-app-bundle-trim-report.json`, and `full-package-boundary-audit.json` to prove explicit non-runtime pruning and boundary preservation; these artifacts do not replace clean VM smoke or owner receipt evidence. v26.6.21 measured `1121919153` Full DMG bytes and a zlib level 9 estimate of `844079932` bytes, so optimization starts with staged App/runtime non-runtime pruning and audit-only dedupe review before compression tuning. Standard and Full profiles stay separate: standard remains updater-visible and must not contain `opl-full-runtime`; Full remains first-install-only with bundled runtime. | `contracts/app-release-channel.json#full_first_install.size_budget`, `contracts/app-release-channel.json#full_first_install.size_policy.optimization_artifacts`, `docs/delivery/release/README.md#full-size-policy`, `scripts/analyze-full-package-size.ts`, `scripts/verify-remote-release-assets.ts`, and release-boundary tests. |
| Full runtime prune policy has one machine source | Full runtime size optimization follows the same maintainability pattern as mature packaging systems: exclusion rules are declarative, package contents have a dry-run/readback path, and app resources remain explicit package-boundary surfaces. The App repo owns `contracts/full-runtime-prune-policy.json` as the single machine-readable prune policy. Build filters, cache keys, Full manifest `runtime_prune_policy`, runtime assertions, and `npm run release:full:prune-audit -- --markdown` derive from that contract; code must not reintroduce divergent hard-coded prune lists. | `contracts/full-runtime-prune-policy.json`, `scripts/full-first-install-package.ts`, `scripts/audit-full-runtime-prune-policy.ts`, `scripts/README.md`, and release-boundary tests. |
| App docs use a lighter lifecycle taxonomy | The App keeps only App-owned long-lived docs directories. Framework/domain-agent taxonomy directories are added only when App material has a clear owner, purpose, state, and machine boundary. | `docs/docs_portfolio_consolidation.md`, `docs/README.md`, and repo docs inventory. |

## Superseded Readings

Rows in this table are retired interpretations kept as compact decision
provenance. They do not define current App behavior; current truth stays in the
`Current reading` column and the listed machine-boundary surfaces.

| Superseded reading | Current reading |
| --- | --- |
| AionUI implementation defaults can define One Person Lab App product truth. | AionUI is implementation material; App product truth is contract-backed from this repo. |
| Upstream AionUI multi-Agent controls are power-user App features. | They are implementation material or diagnostics; the App ordinary path is fixed Codex CLI plus configurable professional-agent shortcuts over installed packages. |
| Upstream AionUI overview/runtime/system/model/agent/tools/webui Settings tabs 是普通 App settings。 | 它们是 legacy 或 diagnostics routes，redirect 到 App-owned Overview、Account & Gateway、Models、Workspace & Personalization、Agents、Capabilities、Resources & Connections、Maintenance、Data & Storage、Preferences 或 About；Advanced 只重定向到 Maintenance diagnostics。 |
| App release or UI rendering can prove MAS/MAG/RCA/BookForge/OMA readiness. | App release artifacts, UI rendering, and provider/read-model availability are App or Framework evidence only; domain readiness remains domain-owned. |
| Full first-install assets can be served through the standard updater channel. | Full assets are separate first-install downloads or the explicit `one-person-lab-full` Homebrew cask and remain outside standard updater metadata. |
| A Homebrew receipt can prove the App is activated or release-ready. | Homebrew proves only that the tap resolved and installed or upgraded indexed files. User-state activation and readiness remain `opl system initialize --json` plus App release gates for the matching cohort. |
| A Nightly Homebrew tap can share the stable/latest formula target. | Nightly distribution remains an explicit prerelease/opt-in lane and points only at Nightly standard assets. It does not mark GitHub latest, select Full assets, or replace stable tap promotion. |
| AionUI builtin skills should be separately classified as App defaults. | AionUI builtin skills are candidate capabilities only. A skill enters the App package only through the App-level packaged whitelist or the explicit-only packaged list. |
| A polished external UI can be copied wholesale into OPL App or define the first screen. | External UIs such as PilotDeck may inform OPL-owned information organization, but source/runtime adoption must pass license, authority, adapter, package, and release-isolation gates first. The ordinary App surface remains chat-first with contextual inspector surfaces. |
| A WebUI candidate can define separate App state or runtime truth because it runs in a browser. | WebUI is only another delivery surface for the same chat-first App UI and must use the same renderer, bridge shape, product profile, Codex backend, and OPL state/action authority boundary. |
| A GUI feature inventory alone is enough to guide shell design. | Shell design must use the App-owned interaction spec and Codex-to-OPL delta first, then the feature inventory and machine contracts for implementation and validation. |
| Hermes Desktop was treated as the foreground alternative. | Hermes Desktop / `hermes-codex` is now a retained reference candidate. The current foreground alternative is `opl-native-workbench`; AionUI remains the active release shell until `contracts/app-shell-adapter.json` changes and release gates pass. |
| AGUI proof should stay archived unless explicitly replayed. | AGUI is archived technical proof and explicit replay material only. It is not the foreground alternative and should not receive routine updates or polish unless AGUI is explicitly requested. |
