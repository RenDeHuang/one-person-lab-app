name: OPL Desktop Release Promote (Retired Compatibility Surface)

on:
  workflow_call:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Reject retired promotion control plane
        run: |
          echo "::error::desktop-release-promote.yml is retired. Latest activation is part of the immutable Release Bundle DAG."
          exit 1
