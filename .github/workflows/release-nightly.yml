name: OPL Nightly Release Bundle

on:
  schedule:
    - cron: '0 13 * * *'

permissions:
  contents: read
  actions: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: opl-release-bundle-global
  cancel-in-progress: false

jobs:
  release:
    permissions:
      contents: write
      actions: read
      packages: write
    uses: ./.github/workflows/_release-bundle.yml
    with:
      channel: nightly
      version: ''
      include_full: false
      app_ref: ${{ github.sha }}
    secrets: inherit
