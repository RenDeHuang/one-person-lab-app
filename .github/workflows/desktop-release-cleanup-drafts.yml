name: OPL Desktop Release Cleanup Drafts (Retired Compatibility Surface)

on:
  workflow_call:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Reject retired draft cleanup mutation
        run: |
          echo "::error::desktop-release-cleanup-drafts.yml is retired. Release reconciliation never deletes or guesses remote state."
          exit 1
