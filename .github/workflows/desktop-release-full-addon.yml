name: OPL Desktop Full Add-on (Retired Compatibility Surface)

on:
  workflow_call:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Reject retired Full publisher
        run: |
          echo "::error::desktop-release-full-addon.yml is retired. Full is an additive track of the immutable Release Bundle."
          exit 1
