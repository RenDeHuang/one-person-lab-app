name: OPL Desktop Release (Retired Compatibility Surface)

on:
  workflow_call:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Reject retired release control plane
        run: |
          echo "::error::desktop-release.yml is retired. Use release-stable.yml and the immutable Framework Release Bundle."
          exit 1
